/**
 * Composition root for the 90 Waypoint Map app.
 *
 * Wired up fully in US-021 (instantiate domain + application, inject the
 * production RandomSource / Renderer / Yield adapters, auto-generate on load).
 * For now this is an empty module so the adapters TypeScript project has an entry point.
 */
export {};
