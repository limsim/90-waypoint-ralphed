/**
 * Temporary placeholder so the domain project has a compilation input.
 * Replaced by the real value objects (Point, Bounds, Heading, ...) from US-002 onwards.
 */
export const DOMAIN_READY = true;
