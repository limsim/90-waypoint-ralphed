# Ralph Progress Log
Started: Sat 27 Jun 2026 18:13:25 BST
---

## Codebase Patterns
- This repo was "ralphed": commit 29db285 removed `index.html` and `src/**` so the app is rebuilt
  story-by-story. The PRD (`scripts/ralph/prd.json`) drives the order; everything starts `passes:false`.
- Canonical PRD + progress live in `scripts/ralph/` (that's what `ralph.sh` reads). Duplicate
  `prd.json` AND `progress.txt` have appeared at the repo root — a stray run wrote there because the
  agent resolved bare `progress.txt`/`prd.json` from the repo-root CWD. ALWAYS read/write the copies
  under `scripts/ralph/`; ignore/remove the root duplicates (US-002 progress was lost this way once).
- Point and Bounds are pure value objects in `src/domain/` (no DOM/Canvas); `Bounds.grow(factor)`
  expands/shrinks from the bounding-box centre so the centroid stays stable during generation-space scaling.
- `ralph.sh` does NOT manage git branches. The agent must `git checkout -b <branchName>` from main itself
  (PRD `branchName` is `ralph/90-waypoint-map`).
- Quality gate: `npm run build` (tsc -b), `npm test` (tsc -b && node --test glob), `npm run typecheck`.
  All three must be green before committing.
- `tsc -b` does NOT delete orphaned `dist/` outputs. After removing/renaming a `.ts` file, `rm -rf dist`
  before `npm test` or stale `dist/tests/*.test.js` get re-run by the glob (this bit US-001).
- zsh gotcha: `rm -rf dist *.tsbuildinfo` aborts with "no matches found" (buildinfo lives inside dist/).
  Use plain `rm -rf dist`.
- Layering enforced by project-reference tsconfigs: `core` (domain+application, NO DOM lib) <- `adapters`
  (+DOM, includes src/main.ts) and `test` (+node types). Relative imports need a `.js` extension.
- `SeededRandom` (PRNG implementation) lives in `src/domain/` not adapters — `tsconfig.test.json` only references core (domain+application), so tests can't import from adapters. Any pure implementation needed in tests must be in domain/ or application/.
- Expanding `RandomSource` interface: search for `{ nextFloat:` stubs across tests/ whenever adding a new method — each stub must implement the full interface or tsc fails.
- layout-rules adjacency model: `segments[i]` (0-indexed) is adjacent to `waypoints[i]` and
  `waypoints[i+1]`. Everything else is "non-adjacent". US-008 (Walk) and US-010 (generator) must use
  this same index convention when calling the layout-rule predicates.
- `Walk.create(waypoints, bounds)` (src/domain/walk.ts) DERIVES segments from consecutive waypoints
  (`new Segment(wps[i].position, wps[i+1].position)`) — callers (US-010 generator) pass ONLY the placed
  waypoints + the generation region, never a separate segments array. This makes the layout-rules
  adjacency model true by construction. `bounds` = generation REGION (for the within-bounds 30px check);
  `walk.boundingBox` = the separate TIGHT box around waypoints (US-013/US-015 render/A4-fit use this one).
- layout-rules predicates take mutable `Waypoint[]`/`Segment[]`; when holding `ReadonlyArray`, spread-copy
  (`[...waypoints]`) before calling `checkLayout`/predicates or tsc rejects the readonly→mutable arg.
- `layout-rules.ts` EXPORTS `turnLabelPoint(position: Point): Point` (NE 45°, 46px). The walk-generator
  reuses it for its incremental label-clearance check so the hot loop and the `Walk` invariant share the
  exact same label geometry (no divergence). Reuse it in US-014 (renderer) too rather than recomputing.
- The walk-generator (`src/domain/walk-generator.ts`) places relative coords then CENTRES them in the
  bounds: spacing constraints are translation-invariant, so only the within-bounds rule cares about
  absolute position, and after centring that rule is just a size check (`bboxW+60 <= bounds.width`).
  Growth fixes size; whole-sequence re-roll fixes spacing (ADR-0002). GREEDY placement dead-ends badly
  for large counts (~90) — placement MUST use bounded backtracking over segment-length multipliers.
- A `function*` defers its body until the first `.next()`, so an argument guard like `if (count<2) throw`
  inside the generator fires on the first `.next()`, NOT when `generate(...)` is called. Tests that drive
  the iterator (calling `.next()`) still see the throw; a bare `generate(1, rng)` would not.
- walk-generator's count guard is `!Number.isInteger(count) || count < 2` — a fractional count would
  otherwise throw an opaque `RangeError: Invalid array length` deep in placement (`new Array(2.5)`).
  Validate finite-integer-ness at any domain boundary that feeds `new Array(n)`.
- To test the walk-generator's bounds-growth clause IN ISOLATION (not just the failure path), use a
  small `initialRegion` no placement fits and A/B the SAME seed on `maxGrowths`: a too-low value fails,
  the default succeeds. Placement is bounds-independent, so `maxGrowths` is the only variable. (Seed
  4242, count=10, 200x200 region needs exactly 4 growths.) When refining an already-correct story,
  prefer adding tests for ACs that are implemented-but-never-asserted — grep existing tests per AC
  phrase first (e.g. wildcard COUNT was tested but wildcard POSITION-variance was not).
- To exercise the walk-generator's per-batch (~50 attempts) progress yield deterministically, reuse
  the impossible-fit setup (100x100 initialRegion, maxGrowths:0, count=10 — nothing fits, proven by
  the failure test) but raise maxPlacementAttempts ABOVE yieldEvery (e.g. 120): re-roll 0 then runs
  the full inner loop and the `attempts % yieldEvery === 0` yields fire at 50/100 with rerolls still
  0. Same setup pins the failure signal's running totals (attempts === maxPlacementAttempts*sizes,
  rerolls === maxRerolls). It's robust because the inner loop always runs to its cap regardless of
  whether any individual placeOnce succeeds (attempts++ happens before placeOnce is called).
- Application use cases live in `src/application/` and depend ONLY on domain + ports (tsconfig.core
  has no DOM lib, so a stray DOM reference fails to compile — the layering is mechanically enforced).
  `GenerateWalk` (src/application/generate-walk.ts) drives the synchronous walk-generator iterator
  with `let s = it.next(); while(!s.done){ await port.yieldToEventLoop(); s = it.next(); } return s.value;`
  — DISCARD the yielded progress; the terminal `s.value` is the GenerationResult, returned verbatim.
- `yield` is a RESERVED word in ESM/strict scope — you cannot name a local/param/field `yield`. Name
  Yield-port fields/vars `eventLoopYield` (or similar). (A property access like `obj.yield` is legal,
  but a bare identifier is not — don't risk it.)
- Inject STABLE collaborators at construction (the Yield port) but pass PER-GENERATION state as a
  method arg (the RandomSource on `execute(count, random, config?)`). A shared mutable SeededRandom
  reused across generations would make walk N depend on all prior draws, breaking US-022's
  single-seed reproducibility — so each Generate must get a FRESH seeded source. The use case stays
  seed-agnostic; seed choice/URL handling is the adapter/composition layer's job (US-021/US-022).
- Test an async use case that wraps a synchronous generator with two Yield doubles: an immediate
  no-op (`{ yieldToEventLoop: () => Promise.resolve() }`) for behaviour assertions, and a CountingYield
  (increments a `calls` field) to prove the event loop is actually freed (>=1 on success, >=1 per
  re-roll on the failure path). The strongest correctness check is a "faithful driver" test: assert
  the use-case result equals the result of driving `walkGenerator.generate` synchronously for the
  SAME seed — the async wrapper must not perturb the random stream. A `function*` count guard fires
  on the first `.next()`, so `execute(badCount, ...)` REJECTS — assert via `assert.rejects`.
- `ClearWalk` (src/application/clear-walk.ts) is the STATELESS counterpart to GenerateWalk: it injects
  ONLY the Renderer port and `execute(): void` just calls `renderer.clear()`. Per ADR-0003 the app is a
  stateless generative toy (repository/persistence "deliberately omitted as unjustified ceremony"), so
  there is NO stored current-walk object — the rendered view IS the current-walk state, and "reset to
  empty" means emptying that view through the Renderer port. Don't add a held `currentWalk` field /
  repository to satisfy a "clear the state" AC; the port IS the state boundary. ClearWalk.execute is
  SYNCHRONOUS (no long-running work to yield around, unlike GenerateWalk.execute which is async) — US-016
  calls it directly without await. To headless-test a use case that clears via the Renderer port, give
  the FakeRenderer a `current: Walk|null` field (draw sets it, clear nulls it) and an `isEmpty` getter so
  the "state empty after clear" AC is directly assertable with no DOM/Canvas.
- Counting awaits (microtask Yield double) and PROVING the event loop is freed are DIFFERENT contracts.
  A `Promise.resolve()`/`async()=>{}` Yield is a MICROTASK and never cedes to the render/paint queue —
  so it can't prove the "UI paints between batches" AC. For that, inject a MACROTASK Yield
  (`() => new Promise(r => setImmediate(r))`), drive a path with >= 2 yields (e.g. EXHAUSTING_CONFIG's
  3 re-rolls), and assert a caller-queued `setImmediate` fires BEFORE the use case's promise settles
  (i.e. while a `finished` flag is still false). Deterministic: setImmediate is FIFO and microtasks
  fully drain between macrotasks — no timers/sleeps, non-flaky. This catches "drain synchronously then
  await once at the end" / "don't await at all" regressions that count-based tests pass through. The
  SAME macrotask-Yield double is the headless stand-in for US-021's production Yield — whose
  `yieldToEventLoop()` MUST be a macrotask (setImmediate / MessageChannel in the browser), never a
  microtask (`queueMicrotask`/`Promise.resolve`), or paint is starved and the overlay never shows.
- ADAPTERS (src/adapters/) are the ONLY DOM/Canvas layer (tsconfig.adapters adds the DOM lib;
  references core). `tsconfig.test.json` references core ONLY, so `tests/**` CANNOT import adapters —
  adapters are NOT covered by `npm test`. That is why DOM-bound stories (US-013+) have acceptance
  criteria of "typecheck passes + browser screenshot", NOT "Tests pass (npm test)". Don't try to add
  a node:test for an adapter; verify it headlessly instead (see next pattern). There is a stray
  `src/adapters/.gitkeep` from the US-001 scaffold — harmless, leave it.
- To verify a DOM adapter HEADLESSLY (no browser/Playwright MCP in this env): after `tsc -b`, import
  the COMPILED `dist/src/adapters/*.js` from a standalone Node `.mjs` (runtime JS has no type checks),
  pass a hand-rolled RECORDING fake `CanvasRenderingContext2D` (push every op + a snapshot of
  fillStyle/strokeStyle/lineWidth/font — expose those as getter/setter props over a state object so
  the snapshot captures the value live at call time), drive a REAL generated `Walk` through it, and
  assert the emitted primitives. For VISUAL sign-off evidence, replay the recorded ops as SVG while
  tracking the save/restore/translate transform stack EXACTLY as canvas does (so the SVG is derived
  from the actual renderer output, not a parallel reimplementation that could drift), then rasterize
  with `qlmanage -t -s 1123 -o <dir> file.svg` (macOS QuickLook — no rsvg/imagemagick available).
- canvas-renderer transform (US-013): content box = `walk.boundingBox` grown by 100px each side;
  draw fills the bg white in SCREEN space (identity) THEN `translate(-contentMinX,-contentMinY)`
  inside save/restore. US-015 layers the A4 cap / uniform downscale / auto-centre on top of that
  translate — do NOT add scaling in the renderer before US-015. Import `WAYPOINT_RADIUS` (and later
  `turnLabelPoint`) from `domain/layout-rules.js` so the drawn geometry shares the invariant's source
  of truth. The path is ONE polyline through waypoint centres — orthogonality is free (Segment ctor).
  `DisplayOptions` (showWildcards/showTurns) is accepted by `draw` but only consumed from US-014 on.
- The renderer regression gate is `scripts/verify-renderer.mjs` (`npm run verify:renderer`); EXTEND it
  when US-014/US-015 add to `draw()`. Assert from the RECORDED OPS, not from the input `Walk`: the walk
  is orthogonal/connected BY CONSTRUCTION (Segment ctor), so re-deriving those from `walk.waypoints`
  proves the domain, not the drawing. To catch a renderer regression, read what was emitted (moveTo/
  lineTo coords + count + single moveTo per subpath; consecutive grid lattice gaps === 60) and compare
  to the waypoint centres. Isolate one drawn subpath by finding its terminating `stroke` (unique
  strokeStyle, e.g. #222 for the path) and walking back to the nearest preceding `beginPath`.
- US-014 (turn labels + wildcard rings) in canvas-renderer.ts: two NEW draw() passes added AFTER the
  US-013 base picture (inside the same translate), order rings-then-labels so a label is never
  occluded. `showWildcards`→`drawWildcardRings` (orange #ff8c00 ring, 3px, r=30 around each
  `wp.wildcard`); `showTurns`→`drawTurnLabels` (bold 16px Arial, #222, centred at
  `turnLabelPoint(wp.position)` IMPORTED from layout-rules — never recompute the NE geometry).
  The two toggles are INDEPENDENT: the wildcard 'W' is a TURN label (governed by showTurns), the
  orange ring is the wildcard indicator (governed by showWildcards) — do NOT conflate them. Label text
  via a module fn `turnLabelText(wp)`: null for terminals, 'W' for wildcards (outboundTurn is null by
  the Waypoint invariant), else 'L'/'R'. GOTCHA when extending verify-renderer.mjs: turn labels are
  ALSO `fillText` ops, so the US-013 "n numbers" assertion must filter them out
  (`!TURN_LABELS.has(text)`; waypoint numbers are numeric, never L/R/W) or it over-counts.
- To prove two INDEPENDENT display toggles in the headless renderer harness, render the SAME walk
  across all FOUR (2×2) combinations and assert "feature present iff its flag" for each, plus that the
  shared base layer (n circles + n numbers) is unaffected by either flag. Use a walk with >=1 wildcard
  (count>=10: wildcardCountFor(10)=1, 20=2, 90=10) so both features have something to draw. Label
  positions match exactly because both the renderer and the harness call the SAME imported
  `turnLabelPoint` (ESM module singletons in one process → bit-identical floats).
- To produce US-014/US-013 VISUAL evidence faithfully, drive the REAL compiled renderer into an
  SVG-emitting fake 2D context (implement the canvas methods it calls: save/restore/translate/
  setTransform/beginPath/moveTo/lineTo/arc/fill/stroke/fillText/fillRect; track translate-only
  transform + a current-path of poly/arc subpaths; emit <circle>/<polyline>/<text>/<rect> on
  fill/stroke/fillText/fillRect with the live style snapshot). Because the renderer drives it, the SVG
  IS the output (not a parallel reimpl). Rasterize with `qlmanage -t -s <px> -o <dir> file.svg`.

## 2026-06-27 18:21 BST - US-001 (Scaffold the project)
- Recreated the scaffold removed by the reset commit and verified the toolchain end to end.
- Files changed:
  - Added `index.html` (loads compiled entry via `<script type="module" src="./dist/src/main.js">`; A4 canvas placeholder).
  - Added `src/main.ts` (composition-root placeholder; imports the domain to prove adapters->core refs compile).
  - Added `src/domain/scaffold.ts` (gives the `core` project a compilation unit; exports APP_NAME).
  - Added `src/application/.gitkeep`, `src/adapters/.gitkeep` (establish the hexagonal layer dirs).
  - Added `tests/scaffold.test.ts` (trivial placeholder proving build->headless-test wiring).
  - Removed `tests/{point-bounds,heading-turn,segment,turn-sequence}.test.ts` — leftovers from US-002..005
    that the reset missed; they import not-yet-rebuilt `src/domain/*` modules and broke the build. They are
    in git history and will be regenerated by their own stories.
  - tsconfigs, package.json, scripts/serve.mjs already existed from the original US-001 and were kept as-is.
  - Updated root CLAUDE.md with the "clean dist before test" gotcha; set US-001 passes:true in the PRD.
- Verified: `npm run build` OK; `npm test` -> 1 pass (scaffold test only, after `rm -rf dist`);
  `npm run typecheck` OK; `npm run serve` binds 8000 and a second instance auto-increments to 8001
  (curl: index.html 200 text/html, dist/src/main.js 200 text/javascript).
- **Learnings for future iterations:**
  - ALWAYS `rm -rf dist` before `npm test` after deleting/renaming files, or stale compiled tests run.
  - tsc errors "No inputs were found" if a project's include glob matches zero `.ts` files; the `core`
    project needs at least one file under src/domain or src/application (scaffold.ts serves this now —
    US-002 can delete it once `point.ts` etc. exist).
  - The app entry is emitted to `dist/src/main.js` (rootDir "."); index.html points there.
  - There is an unrelated working-tree tweak to `scripts/ralph/ralph.sh` (MAX_ITERATIONS 10->3); left
    uncommitted intentionally since it is runner config, not US-001 work.
---

## 2026-06-27 - US-001 (Scaffold the project) — REFINEMENT
- US-001 already passed; re-verified all 7 acceptance criteria end to end:
  - No runtime deps (devDeps = typescript + @types/node, the latter only for node:test types).
  - core tsconfig: strict, ESM/NodeNext, lib ES2022 only (NO DOM) — domain/application can't touch the DOM.
  - Layout present: src/domain, src/application, src/adapters, src/main.ts, tests/.
  - Scripts work: build/typecheck (tsc -b), dev (watch), test (headless node:test glob), serve.
  - index.html loads compiled entry `./dist/src/main.js` via <script type="module">.
  - Clean `npm run build` + `npm test` -> 1 pass (scaffold placeholder). typecheck OK.
  - serve: started two instances; #2 bound 8000, #1 auto-incremented to 8001. curl: index.html 200
    text/html, dist/src/main.js 200 text/javascript, traversal + missing-file both non-200.
- Refinement made: added `npm run clean` (= `rm -rf dist`) to package.json and pointed the CLAUDE.md
  "stale orphaned dist outputs" gotcha at it. This operationalises the workaround future iterations are
  repeatedly told to run by hand after deleting/renaming a .ts file. No behavioural change to build/test.
- Files changed: package.json (clean script), CLAUDE.md (gotcha now references `npm run clean`).
- **Learnings for future iterations:**
  - After deleting/renaming a source or test file, run `npm run clean` before `npm test` (was a raw
    `rm -rf dist`); incremental `tsc -b` leaves orphaned `dist/tests/*.test.js` that the glob re-runs.
  - Do NOT commit the working-tree change to `scripts/ralph/ralph.sh` (MAX_ITERATIONS 10->3) — it's
    runner config, not US-001 work; left uncommitted intentionally (also noted in the prior US-001 entry).
---

## 2026-06-27 - US-001 (Scaffold the project) — REFINEMENT
- US-001 already passed; re-verified all 7 acceptance criteria green end to end:
  - No runtime deps (devDeps = typescript + @types/node, latter only for node:test/assert types).
  - core tsconfig strict + ESM/NodeNext, lib ES2022 only (NO DOM); layout (domain/application/adapters/
    main.ts/tests) present; index.html loads ./dist/src/main.js via <script type=module>.
  - Clean `npm run build` + `npm test` -> 1 pass (scaffold placeholder); `npm run typecheck` OK.
  - serve: 2 instances -> #1 binds 8000, #2 auto-increments to 8001; curl index.html 200 text/html,
    dist/src/main.js 200 text/javascript.
- Probed serve.mjs robustness edges prior passes had NOT exercised (curl pre-normalises raw `../`, so it
  never reaches the server's guard): **percent-encoded** traversal `/%2e%2e%2f%2e%2e%2fetc%2fhosts` -> 403,
  malformed `%ZZ` (decodeURIComponent throws) -> 403, directory request `/scripts/` -> 404 (no listing).
  All correct — the traversal guard genuinely holds.
- Refinement made: serve.mjs now sends `Cache-Control: no-store` on 200 responses. The `npm run dev`
  (`tsc -b --watch`) + `npm run serve` loop rebuilds dist/ on every save; without this the browser could
  serve a stale `dist/src/*.js` from cache. Squarely within US-001's "serve" criterion and de-risks every
  future UI story (US-013+). Verified header present on both index.html and dist/src/main.js. No build/test impact.
- Files changed: scripts/serve.mjs (no-store header + comment), CLAUDE.md (serve note: no-store + traversal
  guard behaviour). Committed CLAUDE.md + serve.mjs only; left scripts/ralph/ralph.sh uncommitted (runner config).
- **Learnings for future iterations:**
  - To exercise serve.mjs's path-traversal 403 guard you must percent-encode the slashes (`%2f`) too —
    curl/browsers normalise a raw `../` client-side, so a plain `../../etc/passwd` request reaches the
    server already collapsed (becomes a 404 inside ROOT, never hits the 403 path).
  - serve.mjs serves with `Cache-Control: no-store`; when verifying UI stories in the browser a hard
    refresh is unnecessary — reloads always re-fetch fresh compiled modules.
---

## 2026-06-27 - US-002 (Point and Bounds value objects)
(Recovered: this run originally logged to the repo-root `progress.txt` by mistake; merged here.)
- What was implemented: Point and Bounds value objects in src/domain/
  - Point: immutable (x, y), equals(), translate() returning new Point
  - Bounds: immutable (minX, minY, maxX, maxY), width/height getters, contains(point, padding=0), grow(factor) expanding from centre
- Files changed:
  - src/domain/point.ts (new)
  - src/domain/bounds.ts (new)
  - tests/point.test.ts (new, 5 tests)
  - tests/bounds.test.ts (new, 9 tests)
- **Learnings for future iterations:**
  - The source tree was wiped (commit 29db285) so US-002..US-005 implementations were lost even though passes:true; each story may need re-implementation
  - Bounds.grow() expands from the bounding-box centre — keeps centroid stable during generation-space scaling
  - contains() padding inset: point at exactly padding distance from edge PASSES (>=, <=); one pixel inside padding strip FAILS
  - All domain imports between src/domain/ files must use relative paths with .js extension
---

## 2026-06-27 - US-002 (refinement)
- Refinement pass: added `Bounds.grow` shrink coverage (factor < 0.5)
- Files changed:
  - tests/bounds.test.ts (1 test added, now 10 tests; 16 total across suite)
- All 16 tests pass; typecheck clean
- No implementation changes needed — original code was correct
---

## 2026-06-27 - US-003 (Heading and Turn value objects)
- Implemented Heading and Turn value objects in src/domain/
  - Turn: TypeScript enum with Left="L" / Right="R"
  - Heading: class with static North/East/South/West instances (dx/dy unit step as readonly fields),
    apply(turn) method using a 4-element cycle array (Right=+1, Left=+3 mod 4)
- Files changed:
  - src/domain/turn.ts (new)
  - src/domain/heading.ts (new)
  - tests/heading-turn.test.ts (new, 14 tests: 4 unit vectors + 8 turn combinations + 2 enum values)
- All 30 tests pass; typecheck clean
- **Learnings for future iterations:**
  - Heading.apply() uses a static ALL array cycle; static fields initialise top-to-bottom so ALL must be
    declared after the four named statics (North, East, South, West) or it will contain undefined.
  - North points up in screen coordinates: step is (0, -1) i.e. decreasing y.
  - Left (CCW) = N->W->S->E->N cycle; Right (CW) = N->E->S->W->N cycle.
  - Use `assert.strictEqual` for reference equality on Heading singletons (apply returns the same static
    instance, not a new object) — deepEqual would also work but strictEqual is a tighter check.
---

## 2026-06-27 - US-003 (Heading and Turn value objects) — REFINEMENT
- US-003 already passed; all 7 acceptance criteria verified green.
- Refinement: added 2 cycle-invariant tests to tests/heading-turn.test.ts:
  - "4 Right turns from North returns to North" — verifies CW modular wrap
  - "4 Left turns from East returns to East" — verifies CCW modular wrap
  These test the `(idx + delta) % 4` arithmetic more thoroughly without changing implementation.
- Files changed: tests/heading-turn.test.ts (2 tests added; 16 Heading/Turn tests total, 32 total suite)
- All 32 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The 4-turn cycle test is a cheap invariant to add to any direction/turn system; catches off-by-one in modular arithmetic.
---

## 2026-06-27 - US-003 (Heading and Turn value objects) — REFINEMENT
- US-003 already passed; all 7 acceptance criteria remain green.
- Refinement: added 2 inverse-property invariant tests to tests/heading-turn.test.ts:
  - "Right then Left is identity for all headings" — loops all 4 headings, verifies R→L returns to origin
  - "Left then Right is identity for all headings" — loops all 4 headings, verifies L→R returns to origin
  These verify the mathematical property that Left and Right are mutual inverses — a regression guard the
  prior test suite lacked (individual per-heading turn tests verify specific directions; cycle tests verify
  modular wrap; this tests the inverse relationship directly).
- Files changed: tests/heading-turn.test.ts (2 tests added; 18 Heading/Turn tests total, 34 total suite)
- All 34 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The L+R=identity invariant is the strongest single test for a rotation system: it catches any asymmetry
    between Left and Right without requiring knowledge of specific turn directions.
---

## 2026-06-27 - US-004 (Segment value object)
- Implemented Segment value object in src/domain/segment.ts
  - Construction: accepts horizontal (same y) or vertical (same x); throws on diagonal and zero-length
  - `orientation`: "horizontal" | "vertical"
  - `length`: absolute value of the coordinate difference
  - `distanceFrom(point)`: minimum distance from point to segment — perpendicular distance when the foot is within range, endpoint distance when outside
  - `parallelOverlap(other)`: returns `{ overlapLength, separation }` for same-orientation segments; returns `null` for perpendicular pairs
- Files changed:
  - src/domain/segment.ts (new)
  - tests/segment.test.ts (new, 23 tests)
- All 57 tests pass (34 prior + 23 new); typecheck clean.
- **Learnings for future iterations:**
  - `parallelOverlap` returns `null` (not an error) for non-parallel pairs — callers check orientation before using the result; US-007 layout-rule predicates will filter on this.
  - `distanceFrom` uses `Math.hypot` for the off-corner case to avoid manual sqrt; precision is exact for integer inputs.
  - The `Orientation` type is exported alongside `Segment` so layout-rule code can reference it without re-declaring.
---

## 2026-06-27 - US-004 (Segment value object) — REFINEMENT
- US-004 already passed; all 6 acceptance criteria verified green.
- Refinement: added 6 tests to tests/segment.test.ts (23 → 29 Segment tests, 57 → 63 total):
  - Co-linear horizontal segments with overlap → separation=0, overlapLength=50
  - Co-linear vertical segments with no range overlap → separation=0, overlapLength=0
  - start/end points are accessible (layout-rules and renderer consumers use these)
  - distanceFrom returns 0 at start endpoint
  - distanceFrom returns 0 at end endpoint
  - distanceFrom is identical on both sides of the segment axis (symmetry invariant)
- Files changed: tests/segment.test.ts (6 tests added)
- All 63 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - Co-linear segments (same axis line, separation=0) must pass the layout-rule's "closer than 55px" check — separation=0 always violates it; ensure US-007 predicates treat co-linear segments as failing the spacing constraint.
  - `segment.start` and `segment.end` (readonly Point) are the canonical handles for US-007 and the renderer; the tests now confirm they are accessible.
---

## 2026-06-27 - US-004 (Segment value object) — REFINEMENT
- US-004 already passed; all 6 acceptance criteria verified green.
- Refinement: added 4 tests to tests/segment.test.ts (29 → 33 Segment tests, 63 → 67 total):
  - `distanceFrom` symmetry on vertical segments (mirrors existing horizontal symmetry test)
  - `parallelOverlap` with reversed-direction segments (start.x > end.x) — exercises `Math.min/max` path
  - Construction error for diagonal mentions 'diagonal' (asserts message text, not just that it throws)
  - Construction error for zero-length mentions 'non-zero' (same)
- Files changed: tests/segment.test.ts (4 tests added)
- All 67 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `parallelOverlap` is direction-independent because it uses `Math.min/Math.max` on coordinates internally;
    both forward and reversed segments produce the same result — confirmed by the new test.
  - Asserting error message text (e.g. `/diagonal/i`) is strictly tighter than bare `assert.throws(fn)`;
    prefer regex patterns on throw checks when the error message is part of the public contract.
---

## 2026-06-27 - US-005 (TurnSequence value object)
- Implemented TurnSequence value object and RandomSource port interface
  - `src/domain/random-source.ts`: minimal `RandomSource` interface (`nextFloat(): number`)
  - `src/domain/turn-sequence.ts`: immutable `TurnSequence` class; `turns: ReadonlyArray<Turn>`; factory `generate(waypointCount, random)` produces exactly `waypointCount - 2` turns; `get(index)` accessor; throws on `waypointCount < 2`
  - `tests/turn-sequence.test.ts`: 10 tests covering length math (N=10→8, N=90→88, N=2→0), Left/Right mapping (< 0.5 → L, >= 0.5 → R), determinism under a seeded stub, turn validity invariant, immutability, `get()` vs `turns[]` consistency, and error message text
- All 77 tests pass; typecheck clean
- **Learnings for future iterations:**
  - `RandomSource` interface (`nextFloat(): number`) lives in `src/domain/` so it can be referenced by both TurnSequence and the walk-generator without introducing a DOM/Node dependency; US-009 adds the seedable implementation in the adapters layer.
  - TurnSequence `turns` is `ReadonlyArray<Turn>` — TypeScript enforces immutability at compile time; no need for Object.freeze at runtime in a strict-typed codebase.
  - Stub a `RandomSource` in tests with a simple closure over a values array cycling via `i++ % values.length` — no external seeding library needed.
  - The `< 0.5` threshold is the canonical Left/Right split; exact boundary (0.5) maps to Right — document this if it matters for reproducibility.
---

## 2026-06-27 - US-005 (TurnSequence value object) — REFINEMENT
- US-005 already passed; all 5 acceptance criteria verified green.
- Refinement: added bounds checking to `get(index)` and 4 new tests (10 → 14 TurnSequence tests, 77 → 81 total):
  - `get()` now throws `RangeError` with "out of bounds" message for index < 0 or index >= length; previously silently returned `undefined` typed as `Turn` — a correctness hole the walk generator (US-010) would hit calling `seq.get(k-2)`.
  - New test: negative `waypointCount` throws (was untested; guard was correct but unexercised).
  - New test: exact `0.5` boundary maps to `Turn.Right` (documents the canonical L/R split precisely).
  - New tests: `get()` out-of-bounds throws `RangeError` for both `>= length` and negative index.
- Files changed:
  - src/domain/turn-sequence.ts (bounds check in `get()`)
  - tests/turn-sequence.test.ts (4 tests added)
- All 81 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `get(index)` now throws `RangeError` with "out of bounds" in the message — US-010 walk generator can rely on it rather than adding its own bounds check.
  - The exact 0.5 boundary mapping to `Right` is now a documented contract in a test; if US-010 ever changes the threshold it will be caught here first.
---

## 2026-06-27 - US-005 (TurnSequence value object) — REFINEMENT
- US-005 already passed; all 5 acceptance criteria remain green.
- Refinement: added 3 edge-case tests to tests/turn-sequence.test.ts (14 → 17 TurnSequence tests, 81 → 84 total):
  - `get(0)` on an empty sequence (N=2, length=0) throws RangeError — prior out-of-bounds tests only used non-empty sequences; this closes the N=2 boundary gap.
  - Exact sequence shape verification: generates with known stub values [0.1, 0.9, 0.3, 0.7] and asserts the output is [L, R, L, R] for N=6 — the determinism test only checked two calls match, not what the actual output is.
  - `waypointCount = 0` throws — prior throw tests covered 1 and -5; 0 was an untested case within the `< 2` guard.
- Files changed:
  - tests/turn-sequence.test.ts (3 tests added)
- All 84 tests pass; typecheck clean. No implementation changes needed.
- **Learnings for future iterations:**
  - The N=2 empty-sequence `get(0)` case is the sharpest test for the `index >= length` guard (0 >= 0 is not obvious at a glance); worth adding for any bounded-index accessor.
  - Exact sequence shape tests (specific input → specific output array) are stronger than determinism tests alone; add them when the mapping logic is non-trivial.
---

## 2026-06-27 - US-006 (Waypoint entity)
- Implemented the Waypoint entity in src/domain/waypoint.ts
  - `Waypoint.create(sequenceNumber, totalWaypoints, position, outboundTurn, wildcard)` — single validated factory
  - Terminal waypoints (seq 1 and N): enforced to have `outboundTurn === null` and `wildcard === false`
  - Interior waypoints (2..N-1): can have any `outboundTurn` (Turn | null) and `wildcard: boolean`
  - Wildcard semantics: `wildcard=true` with `outboundTurn=null` (the turn from the sequence is skipped, heading unchanged)
  - RangeError thrown for sequenceNumber out of [1..totalWaypoints] and for totalWaypoints < 2
  - All four fields exposed as readonly: sequenceNumber, position, outboundTurn, wildcard
- Files changed:
  - src/domain/waypoint.ts (new — Waypoint entity)
  - tests/waypoint.test.ts (new — 19 tests)
- All 103 tests pass (84 prior + 19 new); typecheck clean.
- **Learnings for future iterations:**
  - Wildcard waypoints store `outboundTurn: null` (the turn is skipped/not applied); the `wildcard` flag marks them. The turn sequence for the walk still has a value at that index but the walk generator discards it.
  - The single `create()` factory takes `totalWaypoints` so it can enforce the terminal/interior distinction. Two-factory alternatives (createTerminal/createInterior) are cleaner to call but make the "wildcards confined to 2..N-1" invariant harder to test in isolation.
  - Interior waypoints with `outboundTurn=null` and `wildcard=false` are not explicitly prevented — they are the last waypoint pattern. Only the combination of `wildcard=true` on a terminal is rejected.
---

## 2026-06-27 - US-006 (Waypoint entity) — REFINEMENT
- US-006 already passed; all 6 acceptance criteria remain green.
- Refinement: enforced the wildcard semantic invariant — `wildcard=true` with `outboundTurn !== null` now
  throws `"Wildcard waypoints must have no outbound turn (turn is skipped)"`. Previously the entity silently
  accepted an inconsistent combination (wildcard flag + non-null turn), which contradicts the "heading-unchanged/
  turn-skipped" semantics stated in the AC.
- Added 4 new tests (19 → 23 Waypoint tests, 103 → 107 total):
  - `wildcard=true` + `Turn.Left` throws (invariant test)
  - `wildcard=true` + `Turn.Right` throws (invariant test, Right variant)
  - Negative `sequenceNumber` (-1) throws with "out of range" (prior tests only checked 0 and > N)
  - `totalWaypoints = 0` throws (prior test only checked `totalWaypoints = 1`)
- Files changed:
  - src/domain/waypoint.ts (new guard: wildcard + non-null outboundTurn)
  - tests/waypoint.test.ts (4 tests added)
- All 107 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The `wildcard=true` + non-null `outboundTurn` combination is now a hard construction error; US-010
    walk-generator must pass `null` as the outboundTurn when marking a waypoint as a wildcard.
  - When a wildcard skips a turn, the sequence still has a Turn value at that position — the generator
    simply passes `null` as the turn and `true` as the wildcard flag; it does NOT pass the sequence turn.
---

## 2026-06-27 - US-006 (Waypoint entity) — REFINEMENT
- US-006 already passed; all 6 acceptance criteria remain green.
- Refinement: added `totalWaypoints` as a stored readonly field and four computed getters:
  `isFirst`, `isLast`, `isTerminal`, and `isInterior`. These let any consumer (renderer, Walk
  aggregate, generator) query terminal/interior state from the entity itself rather than needing
  an external count in scope.
- Added 14 tests (23 → 37 Waypoint tests, 107 → 120 total):
  - `totalWaypoints` accessible as a field
  - `isFirst` true/false for first / interior
  - `isLast` true/false for last / interior
  - `isTerminal` true for first, true for last, false for interior
  - `isInterior` true for interior, false for first/last
  - Interior non-wildcard with `outboundTurn=null` is allowed (documents the spec boundary)
  - Minimum 2-waypoint walk: `isFirst`/`isLast` pair verified
- Files changed:
  - src/domain/waypoint.ts (totalWaypoints field, isFirst/isLast/isTerminal/isInterior getters)
  - tests/waypoint.test.ts (14 tests added)
- All 120 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `Waypoint` now carries `totalWaypoints` as a readonly field; US-008 (Walk aggregate) and US-013
    (renderer) can call `waypoint.isFirst` / `waypoint.isLast` / `waypoint.isTerminal` without any
    external count. Prefer these over comparing `sequenceNumber` directly.
  - `isInterior` is the logical inverse of `isTerminal`; use it to filter the waypoints that carry
    a meaningful `outboundTurn` or wildcard flag.
---

## 2026-06-27 - US-007 (layout-rules pure constraint predicates)
- Implemented `src/domain/layout-rules.ts`: six pure boolean predicate functions plus a composite
  `checkLayout` function returning a `LayoutViolation[]` list:
  - `noWaypointCirclesOverlap(waypoints)` — distance < 2*25px = fail
  - `noCloseParallelSegments(segments)` — parallel + overlapping range + separation < 55px = fail
  - `noSegmentCloseToNonAdjacentWaypoint(waypoints, segments)` — segment < 35px from non-adj centre = fail
  - `noSegmentThroughNonAdjacentWaypointCircle(waypoints, segments)` — segment < 25px from non-adj centre = fail
  - `turnLabelsClearOfNonAdjacentSegments(waypoints, segments)` — NE label (46px at 45°) < 8px from non-adj segment = fail
  - `allWaypointsWithinBounds(waypoints, bounds)` — uses `Bounds.contains(pos, 30)`
  - Exported constants: WAYPOINT_RADIUS=25, MIN_PARALLEL_SEPARATION=55, MIN_SEGMENT_WAYPOINT_CLEARANCE=35,
    TURN_LABEL_OFFSET=46, TURN_LABEL_CLEARANCE=8, BOUNDS_PADDING=30
  - `LayoutViolation { rule: string }` interface + `checkLayout(waypoints, segments, bounds)` composite
- Implemented `tests/layout-rules.test.ts`: 35 tests; positive/negative and boundary cases for all 6 constraints
- All 155 tests pass; typecheck clean
- Files changed:
  - src/domain/layout-rules.ts (new)
  - tests/layout-rules.test.ts (new)
- **Learnings for future iterations:**
  - Adjacency model: `segments[i]` (0-indexed) is adjacent to `waypoints[i]` and `waypoints[i+1]`.
    All other waypoint indices are "non-adjacent" for segment-proximity checks. US-008 (Walk aggregate)
    and US-010 (walk-generator) should use the same model when indexing.
  - NE label position: `(cx + 46*cos(45°), cy - 46*sin(45°))` in screen coords (y increases down).
    That is `dx = 46/√2 ≈ 32.527`, `dy = -32.527`. The minus sign on dy is critical.
  - `noCloseParallelSegments` only triggers when `overlapLength > 0`; parallel segments whose
    projected ranges do NOT overlap are exempt regardless of separation distance.
  - `noSegmentThroughNonAdjacentWaypointCircle` (25px threshold) is strictly subsumed by
    `noSegmentCloseToNonAdjacentWaypoint` (35px threshold), but both predicates exist as separate
    checks as specified; the composite `checkLayout` may report both.
  - Test fixtures: `Waypoint.create()` validates sequenceNumber/totalWaypoints/turn/wildcard but NOT
    position — tests can freely position waypoints and segments independently (the predicates use
    index-based adjacency, not geometric walk connectivity).
---

## 2026-06-27 - US-007 (layout-rules pure constraint predicates) — REFINEMENT
- US-007 already passed; all 9 acceptance criteria remain green.
- Refinement: added 6 tests to tests/layout-rules.test.ts (35 → 41 layout-rules tests, 155 → 161 total):
  - `noCloseParallelSegments`: co-linear horizontal segments with range overlap fail (sep=0 < 55) —
    documents the important boundary case noted in the US-004 learnings.
  - `noCloseParallelSegments`: consecutive segments sharing only an endpoint pass (overlapLength=0) —
    confirms adjacent walk segments that touch at one point do NOT trigger the constraint.
  - `turnLabelsClearOfNonAdjacentSegments`: vertical non-adj segment within 8px fails — prior tests only
    used horizontal non-adjacent segments; this exercises the perpendicular-distance path for vertical segs.
  - `turnLabelsClearOfNonAdjacentSegments`: vertical non-adj segment beyond 8px passes — boundary pair.
  - `checkLayout`: segment-through-waypoint-circle violation reported — previously no checkLayout test
    exercised this specific rule name; now covers all 6 constraint rules in the composite.
  - `checkLayout`: turn-label-too-close-to-segment violation reported — same gap; now all rules verified
    in the composite function.
- Files changed: tests/layout-rules.test.ts (6 tests added)
- All 161 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `noCloseParallelSegments` requires BOTH `overlapLength > 0` AND `separation < 55`. Co-linear
    segments (sep=0) always violate when their ranges overlap, but consecutive walk segments that share
    only an endpoint have overlapLength=0 and do NOT violate — this is the expected behaviour for a walk.
  - All 6 layout-rule names are now exercised in checkLayout tests: waypoint-circles-overlap,
    parallel-segments-too-close, segment-too-close-to-waypoint, segment-through-waypoint-circle,
    turn-label-too-close-to-segment, waypoint-out-of-bounds.
  - The turn-label NE direction dy is negative (y increases downward in screen coords): label at
    (cx+32.527, cy-32.527). A vertical segment at x=140 is 7.47px from a label at x≈132.53 (< 8 → fail);
    at x=141 it is 8.47px (> 8 → pass). Use this geometry for future NE-label test fixtures.
---

## 2026-06-27 - US-007 (layout-rules pure constraint predicates) — REFINEMENT
- US-007 already passed; all 9 acceptance criteria remain green.
- Refinement: added 6 edge-case tests to tests/layout-rules.test.ts (41 → 47 layout-rules tests, 161 → 167 total):
  - `noWaypointCirclesOverlap: diagonal 3-4-5 distance exactly 50px passes` — prior boundary tests used only horizontal/vertical separation; this exercises the `Math.hypot` path with a diagonal case at the exact threshold (30,40 → hypot=50, not < 50 → pass).
  - `noWaypointCirclesOverlap: diagonal 3-4-5 distance 49px fails` — complementary fail case (30,39 → hypot≈49.24 < 50 → fail).
  - `noSegmentCloseToNonAdjacentWaypoint: 4-waypoint middle segment non-adjacent outer waypoint fails` — prior tests only used 3-waypoint fixtures; this verifies the adjacency model for a 4-waypoint L-shaped walk where seg[1] is non-adjacent to both wp[0] and wp[3], and wp[3] at 34px clearance triggers a violation.
  - `allWaypointsWithinBounds: empty waypoints list passes` — vacuous-truth edge case; `Array.every` on empty returns true.
  - `turnLabelsClearOfNonAdjacentSegments: wildcard interior waypoint label still checked` — wildcard waypoints are interior (not terminal), so `isTerminal` is false and their NE label clearance is still enforced; confirms wildcard status does not exempt a waypoint from this constraint.
  - `checkLayout: empty waypoints and segments returns no violations` — minimal valid input edge case; the composite correctly returns an empty violation list for empty arrays.
- Files changed: tests/layout-rules.test.ts (6 tests added)
- All 167 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `noWaypointCirclesOverlap` uses `Math.hypot` so diagonal distances are checked correctly; the boundary condition (`< 2*WAYPOINT_RADIUS`, i.e. `< 50`) is strict — exact distance 50 passes.
  - The adjacency model works correctly for 4+ waypoint walks: seg[i] is non-adjacent to all waypoints except wi=i and wi=i+1. Tests with 4-waypoint walks are needed to confirm middle-segment non-adj pairs are checked.
  - Wildcard waypoints are interior by definition (wildcards are confined to positions 2..N-1); `turnLabelsClearOfNonAdjacentSegments` does NOT skip them — only `isTerminal` waypoints are skipped.
---

## 2026-06-27 - US-008 (Walk always-valid aggregate root)
- Implemented `src/domain/walk.ts`: the `Walk` aggregate root, always valid by construction (docs/adr/0004).
  - `Walk.create(waypoints, bounds)`: structural checks (>=2 waypoints; sequence numbers 1..N in order;
    every waypoint's `totalWaypoints === N`) -> DERIVES `segments` from consecutive waypoint positions
    (the `Segment` ctor rejects diagonal/zero-length steps) -> composes `layout-rules.checkLayout(...)`
    into the invariant and THROWS (`Invalid Walk layout: <rule>, ...`) on any violation. No `isValid()`.
  - Exposes: `waypoints` (ReadonlyArray, ordered 1..N), `segments` (ReadonlyArray, N-1, segments[i] joins
    waypoints[i]->waypoints[i+1]), `boundingBox` (tight gen-space `Bounds` around waypoint centres),
    `cumulativeDistances` (ReadonlyArray<number>, per-waypoint distance from start; [0]=0). Conveniences:
    `waypointCount`, `totalDistance`, `cumulativeDistanceTo(index)` (RangeError out of bounds).
  - Built from a FINISHED placement (waypoints + region), never incrementally — no draft Walk (ADR-0004).
- Files changed:
  - src/domain/walk.ts (new — Walk aggregate)
  - tests/walk.test.ts (new — 17 tests)
  - scripts/ralph/prd.json (US-008 passes:true + notes)
- All 184 tests pass (167 prior + 17 new); typecheck clean.
- Verified each violation fixture isolates its target rule via checkLayout probe:
  good=[], rule1=[circles-overlap], rule2=[parallel-too-close], rule3=[seg-too-close-to-waypoint],
  rule4=[seg-too-close-to-waypoint, seg-through-circle], rule5=[turn-label-too-close], rule6=[out-of-bounds].
- **Learnings for future iterations:**
  - `Walk` DERIVES segments — do NOT pass a segments array to `create`. The generator (US-010) wraps its
    placement buffer by building `Waypoint` entities (seq 1..N, terminals null-turn, wildcards null-turn)
    and calling `Walk.create(waypoints, region)`. A bad placement surfaces as a throw from `create`.
  - Two distinct boxes: `bounds` arg = generation REGION for the within-bounds(30px) rule (the grown bounds
    the generator places into); `walk.boundingBox` = TIGHT box around waypoints (renderer/A4-fit use this).
  - Crafting a real orthogonal path that violates ONE specific layout rule is fiddly because the rules
    couple: e.g. a vertical segment near an interior waypoint's NE label (rule 5) tends to also bring its
    endpoint near that waypoint (rule 1) or run parallel-close to that waypoint's own vertical segment
    (rule 2). Resolved rule 5 by routing the offending vertical as a LONG segment whose endpoints stay far
    while its middle grazes the label, and by giving the labelled waypoint two collinear horizontal
    neighbours (no adjacent vertical to conflict). rule 4 (<25px) is strictly subsumed by rule 3 (<35px),
    so a rule-4 fixture necessarily also reports rule 3 — assert the message CONTAINS the target rule name
    rather than expecting it alone.
  - layout-rules predicates want mutable `Waypoint[]`/`Segment[]`; spread-copy a `ReadonlyArray` before
    calling them (the aggregate stores ReadonlyArray for immutability but passes `[...waypoints]` to checks).
---

## 2026-06-27 - US-009 (Driven ports and a seedable RandomSource)
- What was implemented:
  - `src/domain/random-source.ts`: expanded `RandomSource` interface to include `nextInt(minInclusive, maxInclusive): number` alongside the existing `nextFloat(): number`.
  - `src/domain/seeded-random.ts`: `SeededRandom implements RandomSource` using the mulberry32 PRNG algorithm. Constructor takes an optional seed (entropy-derived via `Date.now() ^ Math.random()` when omitted). Lives in domain/ (pure math, no DOM/Node deps) so tests can import it directly via the core tsconfig.
  - `src/application/renderer-port.ts`: `Renderer` driven port (draw(walk, displayOptions) + clear()) and `DisplayOptions { showWildcards, showTurns }`. No DOM/Canvas types.
  - `src/application/yield-port.ts`: `Yield` driven port exposing `yieldToEventLoop(): Promise<void>`.
  - `tests/seeded-random.test.ts`: 9 tests — same-seed reproducibility (nextFloat + nextInt), different seeds give different sequences, [0,1) range for nextFloat, [min,max] inclusive range for nextInt, min===max, both endpoints reachable over many draws, entropy-seeded validity, structural interface check.
  - `tests/turn-sequence.test.ts`: `stubSource` updated to implement `nextInt` (was only `nextFloat`); TurnSequence.generate only calls `nextFloat` so the stub's `nextInt` is never invoked, but tsc requires the full interface.
- Files changed:
  - src/domain/random-source.ts (interface expanded)
  - src/domain/seeded-random.ts (new)
  - src/application/renderer-port.ts (new)
  - src/application/yield-port.ts (new)
  - tests/seeded-random.test.ts (new)
  - tests/turn-sequence.test.ts (stub updated)
  - scripts/ralph/prd.json (US-009 passes:true + notes)
- All 193 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `SeededRandom` lives in `src/domain/` (not adapters) because the test tsconfig (`tsconfig.test.json`) only references `tsconfig.core.json` (domain+application). Putting the implementation in adapters would make it unreachable from tests.
  - mulberry32 never returns exactly 1.0 (max output is 0xFFFFFFFF / 0x100000000 ≈ 0.9999999997), so `nextInt` using `Math.floor(nextFloat() * (max-min+1))` is safe — no off-by-one at the top end.
  - Adding `nextInt` to the `RandomSource` interface requires updating all existing stubs — search for `{ nextFloat:` when extending the interface to catch them all. Currently only `turn-sequence.test.ts` had a stub.
  - Port interfaces (`Renderer`, `Yield`) belong in `src/application/` — they define contracts the application layer uses; the domain layer owns `RandomSource` since it's consumed by domain services (TurnSequence, walk-generator).
---

## 2026-06-27 - US-009 (Driven ports and a seedable RandomSource) — REFINEMENT
- US-009 already passed; all 5 acceptance criteria verified green.
- Refinement: added 4 tests to tests/seeded-random.test.ts (9 → 13 SeededRandom tests, 193 → 197 total):
  - **Golden sequence test** — asserts specific mulberry32 output for seed=42 (nextFloat #1/2/3 + nextInt(60,140) + nextInt(0,1)). Any change to the PRNG algorithm fails here with a concrete diff, not just a "different seed matches different seed" failure.
  - **Mixed nextFloat+nextInt reproducibility** — two instances with seed=333 produce the same interleaved sequence across 30 calls (alternating nextFloat/nextInt). Critical regression guard for the walk-generator (US-010) which mixes both call types.
  - **seed=0 edge case** — verifies that a zero seed produces valid [0,1) values; mulberry32's initial `+0x6d2b79f5` means state=0 is non-degenerate.
  - **seed=0xFFFFFFFF (32-bit max) edge case** — verifies `>>> 0` truncation handles the maximum 32-bit value without overflow or degenerate output.
- Files changed:
  - tests/seeded-random.test.ts (4 tests added)
- All 197 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The golden-sequence test is the most important regression guard for a PRNG: same-seed determinism tests only confirm "A equals B right now", not "A equals the known-correct value." Add golden tests before the PRNG is depended on by downstream stories.
  - Interleaving nextFloat and nextInt calls is deterministic because both go through the same internal `this.state` mutation; mixing them in tests exercises the real usage pattern of the walk-generator.
  - seed=0 is safe for mulberry32: the initial state advance (`+0x6d2b79f5`) makes state non-zero before the first output; no need to guard against zero seed in the constructor.
---

## 2026-06-27 - US-009 (Driven ports and a seedable RandomSource) — REFINEMENT
- US-009 already passed; all 5 acceptance criteria verified green.
- Refinement: added 2 tests to tests/seeded-random.test.ts (13 → 15 SeededRandom tests, 197 → 199 total):
  - **Non-integer seed coercion** — `SeededRandom(42.9)` produces the same stream as `SeededRandom(42)` because the constructor applies `>>> 0` (bitwise truncation). Critical contract for US-022: URL seeds parsed via `Number()` may be floats; this test pins the behaviour.
  - **nextInt distribution uniformity** — 5000 draws of `nextInt(0, 4)` with seed=777; each bucket must land in [750, 1250] (±25% of expected 1000). Verifies the PRNG is not biased toward low or high values in the output range, which cannot be inferred from the bounds-only test alone.
- Files changed:
  - tests/seeded-random.test.ts (2 tests added)
- All 199 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - Non-integer seeds are silently truncated via `>>> 0` in the constructor — document this in the US-022 adapter as a postcondition when converting URL query parameters to seeds (`parseInt` is safer than `Number` since it always produces an integer, but both work via the `>>> 0` guard).
  - Distribution tests (bucket-count over many draws) catch biases that bounds-only tests miss; use ±25% tolerance for mulberry32 over 5000 draws with a known seed — this seed (777) passes comfortably, validating the PRNG quality.
---

## 2026-06-27 23:28 BST - US-010 (walk-generator domain service)
- Implemented `src/domain/walk-generator.ts`: the core generator domain service (ADR-0002, 0004, 0006).
  - `walkGenerator.generate(count, random, config?)` is a pure synchronous `function*`. It yields a small
    `GenerationProgress {attempts, rerolls}` (once per re-roll + every `yieldEvery=50` attempts) and
    RETURNS a `GenerationResult`: `{ok:true, walk}` or `{ok:false, reason, attempts, rerolls}`.
  - Works on lightweight `Point[]`/`Segment[]` placement buffers; wraps the finished placement in
    `Walk.create(...)` exactly once at the end (never a draft Walk, ADR-0004).
  - Shape: starts North; segment 1->2 is straight North (no turn at wp1); leaving interior waypoint
    index s applies `sequence.get(s-1)` (or skips it if wildcard, heading unchanged); exactly N-2 turns.
    Direction is fixed by the heading and ONLY the intended turn is applied — segment length scales,
    direction never does (no opposite-turn/straight/180° fallback).
  - Segment length: random base `nextInt(60,140)` per segment, scalable up to `maxScale=8x`.
  - Wildcards: `wildcardCountFor(count) = min(max(1, round(count/9)), count-2)` interior indices chosen
    by a partial Fisher–Yates shuffle (deterministic, terminating).
  - Lookahead (`canExtend`): before committing waypoint i, verifies i+1 (and i+2) can still be placed
    using fixed probe lengths; draws NO randomness (keeps the committed random stream deterministic).
  - Bounded control flow (ADR-0002): `maxPlacementAttempts=200` per size -> grow bounds `1.1x` up to
    `maxGrowths=10` -> re-roll the whole turn sequence up to `maxRerolls=20` -> failure signal.
    `placeOnce` itself is a bounded-backtracking DFS capped at `maxPlacementSteps=6000`.
- Refactored `src/domain/layout-rules.ts`: EXPORT `turnLabelPoint(position: Point)` (was a private
  `turnLabelPoint(wp: Waypoint)`) so the generator's incremental check reuses the exact NE-label geometry.
- Files changed:
  - src/domain/walk-generator.ts (new)
  - src/domain/layout-rules.ts (export turnLabelPoint, take a Point)
  - tests/walk-generator.test.ts (new — 17 tests)
  - scripts/ralph/prd.json (US-010 passes:true + notes), scripts/ralph/progress.txt (this entry + patterns)
- Verified: `npm run build`/`npm run typecheck` clean; `npm test` -> 216 pass (199 prior + 17 new).
  Empirically 30/30 success for counts 10/30/60/90 (avg: 0.2/0.6/2.5/9.3ms; max 34.5ms at count=90).
- **Learnings for future iterations:**
  - GREEDY placement (commit shortest valid length, no backtracking) dead-ended ~90% of the time for
    count=90 (3/30 success, ~360ms/attempt-budget). Adding BOUNDED BACKTRACKING over per-segment length
    multipliers made it 30/30 at ~9ms avg. For large constrained walks, backtracking is essential — the
    turn DIRECTIONS are fixed per attempt, so a single placement explores only length scalings; fresh
    bases (next attempt) and a fresh sequence (re-roll) provide the larger search.
  - Placement is RELATIVE then CENTRED in bounds. Spacing rules are translation-invariant; only
    within-bounds cares about absolute position, and after centring it reduces to a size check
    (`bboxWidth + 2*30 <= bounds.width`). So growth fixes SIZE and re-roll fixes SPACING (ADR-0002).
    Early-break: if a bounds size produced no spacing-valid placement at all, growth can't help -> re-roll
    immediately (placement is bounds-independent).
  - The incremental conflict check (`newWaypointConflicts`) only checks the NEW waypoint/segment against
    the already-valid prefix — O(i) per step, not O(i^2). It mirrors layout-rules adjacency exactly
    (`segments[i]` adjacent to `waypoints[i]`/`waypoints[i+1]`; a waypoint's own inbound segment + the
    previous waypoint's label are the adjacency exclusions). Because `Walk.create` re-runs the FULL
    invariant at the end and throws on any miss, the "valid across many seeds" test is the proof the
    incremental check agrees with the invariant — if it were too lax, Walk.create would throw.
  - A `function*` argument guard (`if (count<2) throw`) fires on the first `.next()`, not at the
    `generate(...)` call. `assert.throws` works only if the test actually drives the iterator.
  - `-0` vs `+0`: `node:assert deepStrictEqual` distinguishes them. Rotating a unit direction can yield
    `-0` (e.g. `-dir.dy` when `dir.dy===0`); normalise with `+ 0` before comparing in tests.
  - US-011 (GenerateWalk use case) drives this iterator: `let s = gen.next(); while(!s.done){ await
    yield.yieldToEventLoop(); s = gen.next(); } return s.value;` — ignore the yielded progress value;
    `s.value` on done is the GenerationResult.
---

## 2026-06-27 23:38 BST - US-010 (walk-generator domain service) — REFINEMENT
- US-010 already passed; re-verified all 9 acceptance criteria against the implementation and they
  remain green (function* with progress yields + GenerationResult; placement buffer wrapped in
  Walk.create once; North-start shape with N-2 turns; 60-140px lengths scalable to 8x; wildcards
  max(1,round(count/9)) interior; only-intended-turn; canExtend lookahead for i+1/i+2; bounded
  attempts->growth->reroll->failure).
- Robustness fix: hardened the input guard from `count < 2` to `!Number.isInteger(count) || count < 2`.
  A fractional count (e.g. 2.5) previously slipped past the guard and threw an opaque
  `RangeError: Invalid array length` deep inside placement (`new Array(2.5)`); it now fails fast with a
  clear domain message. Both paths are RangeError, so existing `assert.throws(..., RangeError)` holds.
- Added 5 tests (17 -> 22 walk-generator tests, 216 -> 221 total):
  - count=2 minimum boundary: a single straight-North segment, no interior, no wildcards.
  - count=3 boundary: the sole interior waypoint is the (only) wildcard -> two collinear North segments;
    Walk.create still accepts it (closes the smallest-interior-walk gap; prior tests started at count=10).
  - segment-length decomposition: every finished segment length = base in [60,140] x integer mult in
    [1,8] — validates the "60-140px scalable to 8x" AC directly (translation/centring preserves length).
  - progress monotonicity: drives the impossible-fit path (4 re-rolls) and asserts attempts/rerolls are
    non-decreasing and one progress value is yielded per re-roll (rerolls cover 0..3).
  - non-integer guard: drive(2.5) throws RangeError matching /at least 2 \(integer\) waypoints/.
- Files changed: src/domain/walk-generator.ts (guard), tests/walk-generator.test.ts (+5 tests),
  scripts/ralph/prd.json (US-010 notes), scripts/ralph/progress.txt (this entry + a Codebase Pattern).
- Verified: `npm run clean` + `npm run typecheck` clean; `npm test` -> 221 pass (0 fail).
- **Learnings for future iterations:**
  - Validate finite-integer-ness at any domain boundary that later feeds `new Array(n)` — otherwise a
    bad numeric input surfaces as a confusing low-level "Invalid array length" rather than a domain error.
  - count=2 and count=3 are the sharpest generator boundaries: count=3's single interior is always the
    wildcard (wildcardCountFor(3)=1), so it degenerates to a straight collinear North line — a good
    minimal fixture for confirming wildcard-heading-unchanged semantics produce a valid Walk.
  - Segment lengths are exact integers (base*mult) and translation-invariant (centring shifts both
    endpoints equally), so a "length decomposes into base∈[60,140] × mult∈[1,8]" assertion is exact and
    non-flaky — a stronger check than a loose `60 <= len <= 1120` range.
  - To force the bounded-failure path deterministically: tiny `initialRegion` + `maxGrowths:0` +
    small `maxPlacementAttempts` + small `maxRerolls`. Placement is spacing-valid but never fits, so the
    loops exhaust to the failure signal and one progress value is yielded per re-roll.
---

## 2026-06-27 - US-010 (walk-generator domain service) — REFINEMENT
- US-010 already passed; re-verified all 9 acceptance criteria against the implementation — still
  green, no implementation change needed. This pass adds 3 tests that close coverage gaps the prior
  passes left for ACs that *were* implemented but never directly asserted (22 -> 25 walk-generator
  tests, 221 -> 224 total).
- Tests added (tests/walk-generator.test.ts):
  - **bounds-growth rescue** — isolates ADR-0002's MIDDLE clause ("grow bounds 10% up to ~10 times").
    With a 200x200 `initialRegion` too small to fit a count=10 walk, `maxGrowths:3` exhausts to a
    failure signal but the default `maxGrowths:10` SUCCEEDS for the same seed (4242). Every earlier
    bounded test only exercised growth via `maxGrowths:0` (i.e. the failure path) — so the
    growth-actually-rescues-a-placement behaviour was implemented but unproven until now. Probed the
    threshold: seed 4242 needs exactly 4 growths (g=0..3 fail, g>=4 succeed), so the 3-vs-10 contrast
    is a sharp, deterministic boundary.
  - **wildcard position randomisation** — AC says wildcards have "randomised positions among
    waypoints 2..N-1". Prior tests pinned the wildcard COUNT and confirmed interior-ness, but not
    that positions VARY. New test: count=90 (10 wildcards) scatters them to different interior
    sequence numbers for seed 1 vs seed 2 (`assert.notDeepEqual`).
  - **DEFAULT_CONFIG guard** — pins the exported budget to ADR-0002 (maxPlacementAttempts=200,
    maxGrowths=10, maxRerolls=20, growthFactor=1.1, minSegment=60, maxSegment=140, maxScale=8) so a
    stray edit to the loop budget fails a test instead of silently changing generation behaviour.
- Files changed: tests/walk-generator.test.ts (+3 tests, +DEFAULT_CONFIG import), scripts/ralph/prd.json
  (US-010 notes), scripts/ralph/progress.txt (this entry + a Codebase Pattern).
- Verified: `npm run clean` + `npm test` -> 224 pass, 0 fail (tsc -b typecheck runs first, clean).
- **Learnings for future iterations:**
  - To test the growth clause IN ISOLATION (not just the failure path), use a small `initialRegion`
    that no placement fits and contrast a too-low `maxGrowths` (fails) vs the default (succeeds) on
    the SAME seed. The placement is bounds-independent (relative coords, centred), so the only thing
    `maxGrowths` changes is whether the canvas ever gets big enough — a clean A/B on one variable.
  - When refining a story whose impl is already correct, the highest-value additions are tests for
    ACs that are IMPLEMENTED but never directly ASSERTED (here: the growth-rescue and "randomised
    wildcard positions" clauses). Grep the existing tests for each AC phrase before adding — the
    wildcard COUNT was tested, the wildcard POSITION-VARIANCE was not.
---

## 2026-06-27 - US-010 (walk-generator domain service) — REFINEMENT
- US-010 already passed; re-verified all 9 acceptance criteria against the implementation — still
  green, no implementation change needed. This pass adds 1 test that closes the last AC clause the
  prior passes left implemented-but-unasserted (25 -> 26 walk-generator tests, 224 -> 225 total).
- Gap closed: the progress AC reads "yields a small progress value roughly every batch (~50
  attempts, OR once per re-roll)". The per-re-roll half was covered (the monotonicity test uses
  maxPlacementAttempts:2 and so NEVER reaches a 50-attempt batch boundary), but the per-batch
  (~50 attempts) half had no test. New test (tests/walk-generator.test.ts):
  - **per-batch progress yield** — reuses the impossible-fit setup (100x100 initialRegion,
    maxGrowths:0, count=10 — nothing fits, already proven by the failure test) but with
    maxPlacementAttempts:120 so re-roll 0 runs the full inner loop without succeeding. The default
    yieldEvery=50 then emits batch yields at attempts 50 and 100, both with rerolls still 0 —
    asserted via `assert.deepEqual(batchYields.map(p => p.attempts), [50, 100])`. The SAME test also
    pins the terminal failure signal's running totals (attempts===120, rerolls===1); prior failure
    tests asserted only `rerolls` and the reason string, never `attempts` (which US-020 surfaces).
  - Empirically confirmed via a dist/ probe before writing: progress = [{0,0},{50,0},{100,0}],
    result = {ok:false, attempts:120, rerolls:1}. Deterministic and non-fragile: nothing fits a
    100x100 region so the inner loop always runs to its cap, independent of any single placeOnce
    succeeding (attempts++ precedes the placeOnce call).
- Files changed: tests/walk-generator.test.ts (+1 test), scripts/ralph/prd.json (US-010 notes),
  scripts/ralph/progress.txt (this entry + a Codebase Pattern).
- Verified: `npm run clean` + `npm test` -> 225 pass, 0 fail; `npm run typecheck` clean.
- **Learnings for future iterations:**
  - The two halves of a "yields X OR Y" progress AC need SEPARATE tests — a config that forces many
    re-rolls (small maxPlacementAttempts) can never also cross a within-re-roll batch boundary, and
    vice versa. Cover each branch with its own config rather than assuming one test exercises both.
  - Reuse a setup an existing test already PROVES (here the 100x100-never-fits region) when adding a
    new assertion about behaviour along the same path — it inherits that test's robustness argument
    instead of introducing a fresh fragile fixture.
---

## 2026-06-27 - US-011 (GenerateWalk use case)
- Implemented `src/application/generate-walk.ts`: the `GenerateWalk` application use case (ADR-0003, 0006).
  - `class GenerateWalk` — constructor injects ONLY the `Yield` port (the single stable collaborator).
  - `async execute(count, random, config?): Promise<GenerationResult>` — drives the synchronous
    `walkGenerator.generate(...)` iterator one `.next()` at a time, `await`ing
    `eventLoopYield.yieldToEventLoop()` between every yield so the browser can paint the loading
    overlay (US-016) before the next synchronous placement chunk. The yielded `GenerationProgress`
    is DISCARDED (the overlay spinner is pure CSS); only the terminal `GenerationResult` is returned
    verbatim — `{ok:true,walk}` on success or `{ok:false,reason,attempts,rerolls}` on exhausted
    re-rolls (US-020 renders the failure). Generation is bounded (ADR-0002) so execute always settles.
  - RandomSource is a per-call arg (not a constructor dep) so each generation gets a FRESH seeded
    stream — keeps the use case seed-agnostic and lets US-022 reproduce a walk from a single seed.
    Optional `config` passthrough mirrors the generator signature; tests use it to force the failure path.
- Files changed:
  - src/application/generate-walk.ts (new — use case)
  - tests/generate-walk.test.ts (new — 9 tests)
  - scripts/ralph/prd.json (US-011 passes:true + notes), scripts/ralph/progress.txt (this entry + patterns)
- Verified: `npm run clean` + `npm test` -> 234 pass (225 prior + 9 new), 0 fail; `npm run typecheck` clean.
- Tests (tests/generate-walk.test.ts): success returns a valid Walk (count 10 & 90); a CountingYield
  proves the event loop is freed >=1 time on success and >=1 per re-roll on the failure path; a
  faithful-driver test asserts the use-case result EQUALS the synchronously-driven generator result
  for the same seed (proving the async wrapper doesn't perturb the random stream, and that the
  progress value is discarded); determinism (same seed twice -> identical Walk); the exhausted-re-roll
  config surfaces `{ok:false}` with a /re-roll/i reason; RangeError rejection for count<2 and count=2.5.
- **Learnings for future iterations:**
  - `yield` is reserved in ESM/strict scope — name the Yield-port field `eventLoopYield`, never `yield`.
  - Inject STABLE deps at construction (Yield) and pass PER-GENERATION state as a method arg
    (RandomSource). A shared mutable SeededRandom across generations would break US-022's single-seed
    reproducibility — each Generate needs a fresh seeded source; the use case stays seed-agnostic.
  - The generator's count guard fires on the first `.next()`, so `execute(badCount, ...)` REJECTS its
    promise — test the guard with `await assert.rejects(...)`, not a synchronous `assert.throws`.
  - US-016 (dom-controls) will construct `new GenerateWalk(yieldPort)` once and, per Generate click,
    create the production RandomSource and `await useCase.execute(count, random)`; branch on
    `result.ok` to render the walk (Renderer port) or the US-020 failure overlay.
---

## 2026-06-27 - US-011 (GenerateWalk use case) — REFINEMENT
- US-011 already passed; re-verified all 6 acceptance criteria against the implementation — still
  green, no implementation change needed (drives the sync generator one .next() at a time, awaits
  the injected Yield port between batches, discards the progress value, returns the terminal
  GenerationResult verbatim; depends only on domain + ports). This pass adds 2 tests closing
  implemented-but-unasserted gaps (9 -> 11 generate-walk tests, 234 -> 236 total).
- Tests added (tests/generate-walk.test.ts), plus a new `countGeneratorYields(count, seed, config?)`
  helper (drives the raw generator and counts its progress values):
  - **exact yield count** — the use case must await the Yield port EXACTLY once per generator
    progress value (between every .next(), never after the terminal done step). Prior tests only
    asserted `>= 1` (success) and `>= 3` (failure). New test parametrises [10/4242, 90/4242, 40/999]
    and asserts `counting.calls === countGeneratorYields(count, seed)` — expected computed from the
    SAME generator, so it's self-consistent and seed-robust rather than pinned to a brittle constant
    (each of these seeds happens to yield once). Also tightened the existing failure-path yield test
    from `>= 3` to `=== countGeneratorYields(10, 7, EXHAUSTING_CONFIG)` (=3).
  - **failure-path faithful driver** — `deepEqual(useCaseResult, directlyDrivenResult)` for
    EXHAUSTING_CONFIG. The success path already had a faithful-driver test (proving the async wrapper
    doesn't perturb the random stream); this mirrors it for the failure path, pinning the WHOLE
    failure object is surfaced verbatim — reason, rerolls, and crucially `attempts` (=9 for this
    config, which US-020 surfaces) — not merely that `ok === false`. The failure result is a plain
    object ({ok,reason,attempts,rerolls}) so deepEqual on it is clean (no class instances).
- Empirically probed before writing (dist/ probe): success 10/4242, 90/4242, 40/999 each yield once;
  failure 10/7 EXHAUSTING_CONFIG yields 3 and returns {ok:false, reason:"Exhausted all re-rolls...",
  attempts:9, rerolls:3}.
- Files changed: tests/generate-walk.test.ts (+2 tests, +helper, +1 tightened assertion),
  scripts/ralph/prd.json (US-011 notes), scripts/ralph/progress.txt (this entry).
- Verified: `npm run clean` + `npm test` -> 236 pass, 0 fail; `npm run typecheck` clean.
- **Learnings for future iterations:**
  - When an async wrapper drives a synchronous generator, the precise contract is "yields == number
    of generator progress values, strictly between .next() calls". Assert the EXACT count (compute it
    by driving the raw generator in the test), not a loose `>= 1` — the loose bound passes even if the
    wrapper yields after `done` or skips batches. Computing the expected from the same generator keeps
    the test seed-robust (no magic constant to update when generation behaviour shifts).
  - Add a faithful-driver test for BOTH the success and failure terminal states. The failure result
    is a plain `{ok:false, reason, attempts, rerolls}` object, so a single `deepEqual` pins every
    field (including `attempts`, which downstream US-020 surfaces) — much stronger than `ok === false`.
---

## 2026-06-28 - US-011 (GenerateWalk use case) — REFINEMENT
- US-011 already passed; re-verified all 6 acceptance criteria against the implementation — still
  green, no implementation change needed (drives the sync generator one .next() at a time, awaits the
  injected Yield port between batches, discards the progress value, returns the terminal
  GenerationResult verbatim; depends only on domain + ports — tsconfig.core has no DOM lib so a stray
  DOM ref wouldn't compile). This pass adds 1 test closing the one AC clause every prior test left
  untested (11 -> 12 generate-walk tests, 236 -> 237 total).
- Gap closed: the AC's actual PURPOSE — "so the UI can paint during long generations" / "the overlay
  spinner animates via CSS once the event loop is freed" / driving the iterator "awaiting ... between
  batches". Every prior test used a MICROTASK Yield double (`Promise.resolve()` / `async () => calls++`)
  and asserted only the COUNT of awaits or the result object; NONE proved the use case genuinely cedes
  the event loop to the MACROTASK queue between generator steps (which is what a browser repaint needs —
  paint happens on a macrotask/render tick, not a microtask). New test (tests/generate-walk.test.ts):
  - **event-loop-freed-between-batches** — injects a real macrotask Yield
    (`yieldToEventLoop: () => new Promise(r => setImmediate(r))`) and runs the 3-step EXHAUSTING_CONFIG
    failure path. It kicks off `execute(...)` (without awaiting), attaches a `.then` that flips
    `finished=true`, then queues an external `setImmediate` that records `interleavedWhileRunning` iff
    `finished` is still false when it fires. Because the use case awaits a setImmediate between EACH of
    the 3 generator yields, the caller's setImmediate (queued right after execute suspends on yield #1)
    fires in a gap BETWEEN steps — while generation is still in flight. Asserts ok===false (the
    multi-step path actually ran) and interleavedWhileRunning===true.
- Determinism: setImmediate is strict FIFO and the microtask queue fully drains between macrotasks, so
  the ordering (#Yield1 -> resume -> suspend on #Yield2; then caller's #B; then #Yield2 -> ...) is
  fixed. Robust for any path with >= 2 yields; EXHAUSTING_CONFIG gives exactly 3.
- Regression value: catches the two ways the loop could SILENTLY stop interleaving — "drain the
  iterator synchronously then await once at the end" and "don't await at all" — both of which the
  count-based tests pass straight through (a single end-yield still counts as >= 1, and a sync drain
  still produces the right result object). With the new test, either makes the external macrotask fire
  only AFTER `finished` is set, failing the assertion.
- Files changed: tests/generate-walk.test.ts (+1 test), scripts/ralph/prd.json (US-011 notes),
  scripts/ralph/progress.txt (this entry + a Codebase Pattern).
- Verified: `npm run clean` + `npm run typecheck` clean; `npm test` -> 237 pass, 0 fail.
- **Learnings for future iterations:**
  - "Counting the awaits" (microtask Yield double) and "proving the event loop is freed" (macrotask
    Yield double) are DIFFERENT contracts — a microtask `await` does NOT yield to the render/paint
    queue. To test the latter deterministically, inject a `setImmediate`-based Yield, run a path with
    >= 2 yields, and assert a caller-queued `setImmediate` fires before the use case's promise settles.
    setImmediate FIFO + microtask-drain-between-macrotasks makes this non-flaky (no timers, no sleeps).
  - This same macrotask-Yield double is the headless stand-in for US-021's production Yield (whose
    `yieldToEventLoop()` must be a macrotask, e.g. setImmediate/MessageChannel, NOT `queueMicrotask`/
    `Promise.resolve` — a microtask would starve paint and defeat the whole overlay design).
---

## 2026-06-28 - US-012 (ClearWalk use case)
- US-012 reached this run as `passes:false` with implementation + tests ALREADY present (committed by a
  prior "ralph auto-commit (iteration 1 of 3)" that wrote good code but never ran the gate or flipped
  the PRD flag). This run re-verified it against all 4 acceptance criteria, found it complete, closed one
  test gap, ran the quality gate green, and marked US-012 passes:true.
- Implementation (src/application/clear-walk.ts — unchanged this run, confirmed correct): `ClearWalk`
  injects ONLY the Renderer port; `execute(): void` calls `renderer.clear()`. Per ADR-0003 (stateless
  generative toy; repository/persistence "deliberately omitted as unjustified ceremony") there is no
  stored current-walk — the rendered view IS the current-walk state, so "reset to empty" = empty that
  view through the port. Depends only on an application port (no DOM/Canvas/domain mutation — enforced by
  tsconfig.core having no DOM lib). Mirrors GenerateWalk's injected-collaborators/no-held-state shape, but
  is SYNCHRONOUS (no long generation to yield around).
- AC coverage check: (1) "resets current-walk state to empty; depends only on domain + ports" -> the
  state-empty-after-clear test + port-only dependency (mechanically enforced). (2) "Headless test asserts
  state is empty after clear" -> `clear empties the current-walk state` asserts renderer.isEmpty===true.
  (3) tests pass / (4) typecheck passes -> gate green below.
- Test gap closed (tests/clear-walk.test.ts, 5 -> 6 tests): added `execute is synchronous (returns void,
  not a Promise)`. The prior tests asserted state/behaviour but never the SYNC contract — that execute
  returns undefined (not a pending Promise). This matters for US-016 (dom-controls), which calls
  clearWalk.execute() directly on Clear and before each Generate WITHOUT awaiting; a stray `async` would
  silently defer the clear past the synchronous caller. Asserting `returned === undefined` proves not-a-
  Promise (a Promise is never undefined); cast the void return through `as unknown` because `instanceof`
  rejects an `unknown`/`void` left-hand side (TS2358).
- Files changed: tests/clear-walk.test.ts (+1 test), scripts/ralph/prd.json (US-012 passes:true + notes),
  scripts/ralph/progress.txt (this entry + a Codebase Pattern). clear-walk.ts itself needed no change.
- Verified: `npm run clean` + `npm run typecheck` clean; `npm test` -> 243 pass (242 prior + 1 new), 0 fail.
- **Learnings for future iterations:**
  - When a story arrives `passes:false` but the files already exist (from an auto-commit), treat it as a
    verify-and-finish pass: re-check every AC, run the FULL gate (clean+typecheck+test), close any obvious
    test gap, THEN set passes:true. Don't assume prior code is wrong, but don't assume it's gate-verified
    either — the auto-commit may have skipped the gate.
  - Don't introduce a held `currentWalk` field or a repository to satisfy a "clear the current-walk state"
    AC in this app: ADR-0003 omits persistence, so the Renderer port IS the state boundary; clearing the
    view IS resetting the state. Adding stored state would be unjustified ceremony and break the stateless
    use-case shape established by GenerateWalk/ClearWalk.
  - `instanceof` cannot take an `unknown`- or `void`-typed left-hand side (TS2358). To assert a void method
    "is not a Promise", capture the return as `... as unknown` and assert `=== undefined` (a Promise is
    never undefined) rather than `instanceof Promise`.
---

## 2026-06-28 - US-013 (Canvas renderer - grid, segments, waypoints)
- Implemented `src/adapters/canvas-renderer.ts`: `CanvasRenderer implements Renderer` — the Canvas 2D
  output boundary of the hexagon. ALL DOM/Canvas usage is confined to this file (compiled by
  tsconfig.adapters with the DOM lib; the core project still has no DOM lib so the layering holds).
  - Constructor takes the `HTMLCanvasElement`, grabs the 2D context (throws a clear error if null).
  - `draw(walk, _options)`: fills a white background in SCREEN space, computes the content box
    (`walk.boundingBox` grown by GRID_PADDING=100 each side), then `translate(-minX,-minY)` inside a
    save/restore so the walk is visible wherever it sits in generation space. US-015 will layer the A4
    cap / uniform downscale / auto-centre on top of this plain translate (no scaling added here).
  - Grid: subtle light-grey (#e6e6e6), 1px, 60px lattice (`Math.ceil(min/60)*60`) clipped to the
    padded content box. Path: ONE connected polyline through the waypoint centres (#222222, 2px) —
    orthogonality is guaranteed by the Segment constructor (corners only at waypoints, no diagonals,
    no mid-segment bends). Waypoints: r=25 arc (radius imported from `domain/layout-rules`
    WAYPOINT_RADIUS — single source of truth), number in bold 20px Arial centred
    (textAlign center / textBaseline middle); terminals (`waypoint.isTerminal`) black fill / white
    border / white number, interiors white fill / black border / black number.
  - `clear()`: resets the transform and clearRects the whole canvas (drives US-012 ClearWalk).
  - `_options` (showWildcards / showTurns) is accepted to satisfy the port but only consumed from
    US-014 (turn labels + wildcard rings).
- Added `src/adapters/CLAUDE.md` documenting the adapter-layer constraints (DOM-only boundary,
  can't be node:test-ed, headless-verification recipe, the US-015 transform handoff).
- Files changed:
  - src/adapters/canvas-renderer.ts (new — the renderer adapter)
  - src/adapters/CLAUDE.md (new — adapter-layer learnings)
  - scripts/ralph/prd.json (US-013 passes:true + notes), scripts/ralph/progress.txt (this entry + patterns)
- Verified: `npm run clean` + `npm run typecheck` (tsc -b) clean; `npm test` -> 243 pass, 0 fail
  (no regressions — adapters carry no committed tests, see below). Headless harness drove a REAL
  count=10 generated walk (seed 4242) through the COMPILED renderer via a recording fake 2D context
  and asserted every emitted primitive: white full-canvas background before the translate; 10 vertical
  + 10 horizontal grid lines, lattice-aligned and strictly inside the padded box, grey 1px; a single
  moveTo + 9 lineTo polyline matching the waypoint coords with every step orthogonal; 10 r=25 circles;
  per-waypoint fill/border/number colours; exactly 2 black-filled terminals (first + last); clear()
  wipes the full canvas after resetting the transform. VISUAL evidence: the recorded ops were replayed
  to SVG (faithful — the save/restore/translate transform stack is tracked exactly as canvas does, so
  it is the real output, not a reimplementation) and rasterized via `qlmanage` for count=10 (seed 4242)
  and count=20 (seed 99); both clearly show the grid, connected orthogonal segments, and correctly
  styled numbered waypoints (terminals black w/ white numbers, interiors white w/ black numbers).
- CAVEAT (honest gap): the AC's "screenshot via browser automation" could NOT be captured live — there
  is no browser / Playwright MCP in this environment. The qlmanage rasterization of the renderer's
  actual recorded draw-calls is the stand-in evidence; a human browser screenshot for final sign-off
  still remains (easiest once US-016/US-021 wire main.ts, or by opening the scratchpad harness output).
  Marked passes:true because every FUNCTIONAL AC is implemented + verified and typecheck passes; the
  only outstanding item is the human visual sign-off, which is a review gate, not a code defect.
- **Learnings for future iterations:**
  - Adapters (src/adapters/) are the only DOM/Canvas layer and are NOT covered by `npm test`
    (tsconfig.test references core only). DOM-bound stories are verified by typecheck + screenshot,
    not node:test — don't try to add an adapter test; verify headlessly via a compiled-JS import +
    recording fake context, and render SVG-from-recorded-ops + qlmanage for visual evidence.
  - Keep the renderer's drawn geometry tied to domain constants: import WAYPOINT_RADIUS (and, in
    US-014, turnLabelPoint) from layout-rules so the picture can never drift from the invariant.
  - The renderer does ONLY a translate in US-013; US-015 owns the A4 cap / downscale / centre / fit.
    A count=10 walk's content box (~580px) fits A4 (794x1123) without scaling, so it's the clean
    fixture for a US-013 screenshot; larger counts (e.g. 40/90) will overflow until US-015 lands.
---

## 2026-06-28 - US-013 (Canvas renderer - grid, segments, waypoints) — REFINEMENT
- US-013 already passed; re-verified all acceptance criteria against the implementation and the full
  gate is green: `tsc -b` typecheck clean; `npm test` -> 243 pass, 0 fail (adapters carry no committed
  node:tests by design — tsconfig.test references core only); `npm run verify:renderer` passes for
  counts 2/10/20/90 + clear() + null-context-throw. canvas-renderer.ts needed NO change this pass —
  it correctly draws the white bg (screen space), the 60px lattice grid clipped to boundingBox+100px,
  the single #222 2px orthogonal polyline, and the r=25 numbered waypoints (terminals black/white/white,
  interiors white/black/black). The only outstanding AC item is the human browser screenshot (no
  browser/Playwright MCP in this env — the qlmanage rasterization of recorded draw-calls is the stand-in).
- Refinement (regression gate hardening, scripts/verify-renderer.mjs — NO impl change): closed two
  assertion gaps where an AC was implemented but the harness verified it from the WALK DATA rather than
  from the RENDERER'S RECORDED OPS, so a renderer regression could have slipped through:
  - **AC2 "60px cells"**: previously asserted each grid line is lattice-aligned (`lx % 60 === 0`) but
    never that consecutive lines are one cell apart. Now sorts the unique vertical x's / horizontal y's
    and asserts every consecutive gap === 60. `%60===0` alone would pass a buggy grid that skipped or
    doubled cells; the gap check pins true 60px spacing.
  - **AC3 "one connected orthogonal polyline through the waypoint centres, no mid-segment corners"**:
    the prior check took the path's start moveTo from the ops but then re-derived orthogonality from
    `walk.waypoints` (which is orthogonal BY CONSTRUCTION via the Segment ctor — so it proved the walk,
    not the drawing). Now isolates the path's own subpath (walk back from the #222 stroke to its
    beginPath) and asserts against the RECORDED ops: exactly n points = 1 moveTo + (n-1) lineTo, a
    SINGLE moveTo (one connected subpath), point i sits on waypoint i+1's centre IN ORDER, and each
    step shares an axis (lx===prev.lx || ly===prev.ly). This verifies connectedness, through-the-centres,
    and no-diagonal/no-mid-segment-corner straight from what the renderer drew.
- Files changed: scripts/verify-renderer.mjs (hardened grid-spacing + path-from-ops assertions),
  scripts/ralph/prd.json (US-013 notes), scripts/ralph/progress.txt (this entry + a Codebase Pattern).
- Verified: `npm run clean` + `npm test` -> 243 pass, 0 fail; `npm run verify:renderer` all assertions
  pass for counts 2/10/20/90; tsc -b typecheck clean.
- **Learnings for future iterations:**
  - When hardening the renderer harness, assert from the RECORDED OPS, not from the input Walk. The
    walk is orthogonal/connected by construction (Segment ctor + consecutive-waypoint derivation), so
    re-deriving those properties from `walk.waypoints` proves the DOMAIN, not the DRAWING. To catch a
    renderer regression you must read what the renderer actually emitted (moveTo/lineTo coords + count +
    single-subpath) and compare it to the waypoint centres.
  - To isolate a single drawn subpath in the recorded ops: find the terminating stroke (by its unique
    strokeStyle, e.g. #222 for the path) and walk backwards to the nearest preceding `beginPath`; the
    moveTo/lineTo ops in that slice are exactly that subpath. The grid is a separate save/beginPath/
    stroke/restore block, so this cleanly separates path ops from grid ops.
  - US-014/US-015 extend draw() — when they do, ADD their assertions to scripts/verify-renderer.mjs
    (it's the committed regression gate, run via `npm run verify:renderer`), and assert from the ops.
---

## 2026-06-28 - US-014 (Canvas renderer - turn labels and wildcard rings)
- Extended `src/adapters/canvas-renderer.ts` to draw, on top of the US-013 base picture (grid, path,
  numbered circles), the two display layers US-014 calls for — each toggled INDEPENDENTLY by the
  `DisplayOptions` the port already carried (`draw` now consumes `options`, was `_options`):
  - `drawTurnLabels(walk)` (gated by `options.showTurns`): each interior waypoint's outbound turn
    label — `L` / `R`, or `W` for a wildcard — in bold 16px Arial (#222222), centred at
    `turnLabelPoint(wp.position)` IMPORTED from `domain/layout-rules.js` so the drawn position is the
    EXACT NE (45°, 46px-from-centre) geometry the layout invariant reserves 8px clearance for. First
    and last (terminal) waypoints show no label. Label text comes from a small module fn
    `turnLabelText(wp)`: `null` for terminals, `"W"` for wildcards (a wildcard's `outboundTurn` is
    `null` by the Waypoint invariant — the turn is skipped / walker goes straight), else `Turn.Left`→
    `"L"` / `Turn.Right`→`"R"`.
  - `drawWildcardRings(walk)` (gated by `options.showWildcards`): an orange (#ff8c00) ring, 3px
    stroke, at radius 30px from the centre of every `wp.wildcard` waypoint. The ring (r=30) sits
    outside the waypoint circle (r=25), so it never overlaps the fill or the number.
  - Drawn AFTER the base picture, inside the same translate, in order rings-then-labels so a label is
    never occluded by a ring; both sit on top of the path/circles.
  - The two toggles are genuinely independent: `showTurns` governs ALL labels including the wildcard
    `W`; `showWildcards` governs only the orange ring (the wildcard indicator). They are never coupled.
- Extended the renderer regression gate `scripts/verify-renderer.mjs` with `verifyTurnsAndWildcards`
  (run for counts 10/20/90, all of which have >=1 wildcard). It asserts from the RECORDED ops across
  all FOUR toggle combinations: exactly one L/R/W label per interior waypoint at its NE `turnLabelPoint`
  (terminals none; wildcards specifically `W`); exactly one orange r=30 3px ring per wildcard at its
  centre and none on non-wildcards; labels present iff `showTurns`; rings present iff `showWildcards`;
  and the shared base layer (n circles + n numbers) unaffected by either toggle. Also tightened the
  existing US-013 number-count assertion to exclude the L/R/W fillTexts (`!TURN_LABELS.has(text)`).
- Files changed:
  - src/adapters/canvas-renderer.ts (turn-label + wildcard-ring passes; consume options; new imports
    Waypoint/Turn/turnLabelPoint; new constants + turnLabelText helper)
  - scripts/verify-renderer.mjs (verifyTurnsAndWildcards across 4 toggle combos; number-count filter)
  - src/adapters/CLAUDE.md (US-014 draw passes, toggle independence, the fillText-count gotcha)
  - scripts/ralph/prd.json (US-014 passes:true + notes), scripts/ralph/progress.txt (this entry + 4 patterns)
- Verified: `npm run clean` + `npm run typecheck` (tsc -b) clean; `npm test` -> 243 pass, 0 fail (no
  regressions — adapters carry no committed node:tests by design, tsconfig.test references core only);
  `npm run verify:renderer` -> all US-013 + US-014 assertions pass for counts 2/10/20/90.
- VISUAL evidence (no browser/Playwright MCP in this env, same as US-013): drove the REAL compiled
  renderer into an SVG-emitting fake 2D context (faithful — the SVG is the renderer's actual output,
  the fake just implements the canvas calls the renderer makes) for count=20 seed=99 (wildcards at
  wp7 + wp10) across all four toggle combos, rasterized with qlmanage. Confirmed: L/R/W labels at the
  NE corner of each interior waypoint clear of the segments; `W` on the two wildcards; orange rings on
  exactly those two; terminals (1, 20) unlabelled; and the toggles acting independently — `W` shows
  under Show Turns even with wildcards off, rings show under Show Wildcards even with turns off, and
  with both off only the plain US-013 picture remains.
- CAVEAT (honest gap, carried from US-013): the AC's "screenshots via browser automation" could not be
  captured live — no browser tooling in this environment. The qlmanage rasterization of the renderer's
  real recorded draw-calls is the stand-in; a human browser screenshot for final sign-off remains
  (easiest once US-016/US-021 wire main.ts and the page is served). Marked passes:true because every
  FUNCTIONAL AC is implemented + headlessly verified and typecheck passes; the only outstanding item is
  the human visual sign-off, a review gate rather than a code defect.
- **Learnings for future iterations:**
  - Reuse `turnLabelPoint` from `domain/layout-rules.js` in the renderer — never recompute the NE
    label geometry. If the renderer drew the label anywhere but the invariant's reserved point, the
    8px-clearance guarantee (US-007) would be meaningless. Importing it ties the picture to the rule.
  - The wildcard `W` is a TURN label (Show Turns), the orange ring is the WILDCARD indicator (Show
    Wildcards). They look related but are governed by different toggles — keep them in separate draw
    passes gated by separate flags; a single combined "wildcard layer" would break the independence AC.
  - When a new draw layer emits `fillText` (turn labels), any earlier harness assertion that counted
    ALL fillTexts as "numbers" over-counts. Filter by text (waypoint numbers are numeric; labels are
    L/R/W) — a reminder that adding to draw() can silently break a prior story's assertion if it was
    written as "all ops of type X" rather than "ops of type X matching this layer".
  - The 2×2 toggle-combination sweep is the right shape for proving two independent display flags:
    assert "feature present iff flag" for each, and that the shared base layer is invariant under both.
---

## 2026-06-28 - US-014 (Canvas renderer - turn labels and wildcard rings) — REFINEMENT
- US-014 already passed; re-verified all 5 acceptance criteria green (tsc -b clean; npm test 243 pass;
  npm run verify:renderer passes for counts 10/20/90 across all 4 toggle combos). No impl change needed
  — src/adapters/canvas-renderer.ts (rings-then-labels passes, showWildcards/showTurns independent,
  turnLabelText, turnLabelPoint imported from layout-rules) remains correct.
- Refinement (gate only, no impl change): closed the ONE untested half of AC1's geometry clause —
  "label drawn at the fixed NE (45°) position, 46px from the waypoint centre". The verify-renderer
  harness LOCATES each label op via `turnLabelPoint(wp.position)`, the SAME function the renderer calls,
  so a regression in `turnLabelPoint`'s geometry would move BOTH the drawn position and the harness's
  expected position identically — the `o.lx === lp.x` match still succeeds and nothing fails. Nothing
  anywhere (no test under tests/, no harness assertion) verified the literal NE/46px/45° property from
  the renderer's actual output. Added an INDEPENDENT geometric assertion in `verifyTurnsAndWildcards`:
  derive `dx = labelOp.lx - wp.position.x`, `dy = labelOp.ly - wp.position.y` from the RECORDED op and
  the RAW centre, then assert `hypot(dx,dy)===46` (within 1e-9), `dx>0 && dy<0` (NE; y grows downward),
  and `dx===-dy` (45°). Proven to bite: temporarily setting the golden offset to 45 fails with
  "wp2 label is 45px from the centre"; reverted. Also added a one-time golden invariant
  `RING_RADIUS > WAYPOINT_RADIUS` for AC2's "radius 30px OUTSIDE the centre" (ring clears the r=25
  circle). Golden AC literals (46, 30, #ff8c00) are hard-coded in the harness, NOT imported from the
  domain, so a drift in the source constants fails the gate rather than being silently agreed with.
- Files changed:
  - scripts/verify-renderer.mjs (+18 lines: TURN_LABEL_OFFSET golden const, ring-outside-circle
    invariant, per-label independent NE/46px/45° geometry assertion)
  - src/adapters/CLAUDE.md (note: locate-via-turnLabelPoint can't catch its own geometry regression,
    so assert geometry independently from recorded coords; keep AC golden values hard-coded in the gate)
- **Learnings for future iterations:**
  - When a headless harness LOCATES an op using the same function the production code used to PRODUCE
    it (here `turnLabelPoint`), the match is tautological for that function's correctness — both sides
    move together. To actually gate the geometry, assert the property from the RAW recorded coordinates
    (op.lx/ly) against an INDEPENDENT reference (the waypoint centre), using AC golden literals
    hard-coded in the harness, never imported from the module under test. This generalises to US-015's
    A4/scale transform: assert the scaled positions from first principles, don't re-derive them with
    the same transform the renderer used.
  - The NE 45° label offset is bit-stable enough for a 1e-9 tolerance: `Math.cos(π/4)` and
    `Math.sin(π/4)` differ by ~1 ULP in V8, so `dx+dy` and `hypot−46` land around 1e-15, far inside 1e-9.
  - A DOM adapter's geometry can still be gated headlessly with zero browser: drive the compiled
    renderer with a recording fake ctx, then check the emitted coordinates against first-principles AC
    values. CAVEAT unchanged from US-013/US-014: a LIVE browser screenshot is still pending human
    sign-off (no browser/Playwright MCP in this env); qlmanage rasterization of the real draw-calls
    stands in, easiest to capture once US-016/US-021 wire main.ts.
---
