# Ralph Progress Log
Started: Sat 27 Jun 2026 18:13:25 BST
---

## Codebase Patterns
- This repo was "ralphed": commit 29db285 removed `index.html` and `src/**` so the app is rebuilt
  story-by-story. The PRD (`scripts/ralph/prd.json`) drives the order; everything starts `passes:false`.
- Canonical PRD + progress live in `scripts/ralph/` (that's what `ralph.sh` reads). Duplicate
  `prd.json` AND `progress.txt` have appeared at the repo root — a stray run wrote there because the
  agent resolved bare `progress.txt`/`prd.json` from the repo-root CWD. ALWAYS read/write the copies
  under `scripts/ralph/`; ignore/remove the root duplicates (US-002 progress was lost this way once).
- Point and Bounds are pure value objects in `src/domain/` (no DOM/Canvas); `Bounds.grow(factor)`
  expands/shrinks from the bounding-box centre so the centroid stays stable during generation-space scaling.
- `ralph.sh` does NOT manage git branches. The agent must `git checkout -b <branchName>` from main itself
  (PRD `branchName` is `ralph/90-waypoint-map`).
- Quality gate: `npm run build` (tsc -b), `npm test` (tsc -b && node --test glob), `npm run typecheck`.
  All three must be green before committing.
- `tsc -b` does NOT delete orphaned `dist/` outputs. After removing/renaming a `.ts` file, `rm -rf dist`
  before `npm test` or stale `dist/tests/*.test.js` get re-run by the glob (this bit US-001).
- zsh gotcha: `rm -rf dist *.tsbuildinfo` aborts with "no matches found" (buildinfo lives inside dist/).
  Use plain `rm -rf dist`.
- Layering enforced by project-reference tsconfigs: `core` (domain+application, NO DOM lib) <- `adapters`
  (+DOM, includes src/main.ts) and `test` (+node types). Relative imports need a `.js` extension.
- layout-rules adjacency model: `segments[i]` (0-indexed) is adjacent to `waypoints[i]` and
  `waypoints[i+1]`. Everything else is "non-adjacent". US-008 (Walk) and US-010 (generator) must use
  this same index convention when calling the layout-rule predicates.

## 2026-06-27 18:21 BST - US-001 (Scaffold the project)
- Recreated the scaffold removed by the reset commit and verified the toolchain end to end.
- Files changed:
  - Added `index.html` (loads compiled entry via `<script type="module" src="./dist/src/main.js">`; A4 canvas placeholder).
  - Added `src/main.ts` (composition-root placeholder; imports the domain to prove adapters->core refs compile).
  - Added `src/domain/scaffold.ts` (gives the `core` project a compilation unit; exports APP_NAME).
  - Added `src/application/.gitkeep`, `src/adapters/.gitkeep` (establish the hexagonal layer dirs).
  - Added `tests/scaffold.test.ts` (trivial placeholder proving build->headless-test wiring).
  - Removed `tests/{point-bounds,heading-turn,segment,turn-sequence}.test.ts` — leftovers from US-002..005
    that the reset missed; they import not-yet-rebuilt `src/domain/*` modules and broke the build. They are
    in git history and will be regenerated by their own stories.
  - tsconfigs, package.json, scripts/serve.mjs already existed from the original US-001 and were kept as-is.
  - Updated root CLAUDE.md with the "clean dist before test" gotcha; set US-001 passes:true in the PRD.
- Verified: `npm run build` OK; `npm test` -> 1 pass (scaffold test only, after `rm -rf dist`);
  `npm run typecheck` OK; `npm run serve` binds 8000 and a second instance auto-increments to 8001
  (curl: index.html 200 text/html, dist/src/main.js 200 text/javascript).
- **Learnings for future iterations:**
  - ALWAYS `rm -rf dist` before `npm test` after deleting/renaming files, or stale compiled tests run.
  - tsc errors "No inputs were found" if a project's include glob matches zero `.ts` files; the `core`
    project needs at least one file under src/domain or src/application (scaffold.ts serves this now —
    US-002 can delete it once `point.ts` etc. exist).
  - The app entry is emitted to `dist/src/main.js` (rootDir "."); index.html points there.
  - There is an unrelated working-tree tweak to `scripts/ralph/ralph.sh` (MAX_ITERATIONS 10->3); left
    uncommitted intentionally since it is runner config, not US-001 work.
---

## 2026-06-27 - US-001 (Scaffold the project) — REFINEMENT
- US-001 already passed; re-verified all 7 acceptance criteria end to end:
  - No runtime deps (devDeps = typescript + @types/node, the latter only for node:test types).
  - core tsconfig: strict, ESM/NodeNext, lib ES2022 only (NO DOM) — domain/application can't touch the DOM.
  - Layout present: src/domain, src/application, src/adapters, src/main.ts, tests/.
  - Scripts work: build/typecheck (tsc -b), dev (watch), test (headless node:test glob), serve.
  - index.html loads compiled entry `./dist/src/main.js` via <script type="module">.
  - Clean `npm run build` + `npm test` -> 1 pass (scaffold placeholder). typecheck OK.
  - serve: started two instances; #2 bound 8000, #1 auto-incremented to 8001. curl: index.html 200
    text/html, dist/src/main.js 200 text/javascript, traversal + missing-file both non-200.
- Refinement made: added `npm run clean` (= `rm -rf dist`) to package.json and pointed the CLAUDE.md
  "stale orphaned dist outputs" gotcha at it. This operationalises the workaround future iterations are
  repeatedly told to run by hand after deleting/renaming a .ts file. No behavioural change to build/test.
- Files changed: package.json (clean script), CLAUDE.md (gotcha now references `npm run clean`).
- **Learnings for future iterations:**
  - After deleting/renaming a source or test file, run `npm run clean` before `npm test` (was a raw
    `rm -rf dist`); incremental `tsc -b` leaves orphaned `dist/tests/*.test.js` that the glob re-runs.
  - Do NOT commit the working-tree change to `scripts/ralph/ralph.sh` (MAX_ITERATIONS 10->3) — it's
    runner config, not US-001 work; left uncommitted intentionally (also noted in the prior US-001 entry).
---

## 2026-06-27 - US-001 (Scaffold the project) — REFINEMENT
- US-001 already passed; re-verified all 7 acceptance criteria green end to end:
  - No runtime deps (devDeps = typescript + @types/node, latter only for node:test/assert types).
  - core tsconfig strict + ESM/NodeNext, lib ES2022 only (NO DOM); layout (domain/application/adapters/
    main.ts/tests) present; index.html loads ./dist/src/main.js via <script type=module>.
  - Clean `npm run build` + `npm test` -> 1 pass (scaffold placeholder); `npm run typecheck` OK.
  - serve: 2 instances -> #1 binds 8000, #2 auto-increments to 8001; curl index.html 200 text/html,
    dist/src/main.js 200 text/javascript.
- Probed serve.mjs robustness edges prior passes had NOT exercised (curl pre-normalises raw `../`, so it
  never reaches the server's guard): **percent-encoded** traversal `/%2e%2e%2f%2e%2e%2fetc%2fhosts` -> 403,
  malformed `%ZZ` (decodeURIComponent throws) -> 403, directory request `/scripts/` -> 404 (no listing).
  All correct — the traversal guard genuinely holds.
- Refinement made: serve.mjs now sends `Cache-Control: no-store` on 200 responses. The `npm run dev`
  (`tsc -b --watch`) + `npm run serve` loop rebuilds dist/ on every save; without this the browser could
  serve a stale `dist/src/*.js` from cache. Squarely within US-001's "serve" criterion and de-risks every
  future UI story (US-013+). Verified header present on both index.html and dist/src/main.js. No build/test impact.
- Files changed: scripts/serve.mjs (no-store header + comment), CLAUDE.md (serve note: no-store + traversal
  guard behaviour). Committed CLAUDE.md + serve.mjs only; left scripts/ralph/ralph.sh uncommitted (runner config).
- **Learnings for future iterations:**
  - To exercise serve.mjs's path-traversal 403 guard you must percent-encode the slashes (`%2f`) too —
    curl/browsers normalise a raw `../` client-side, so a plain `../../etc/passwd` request reaches the
    server already collapsed (becomes a 404 inside ROOT, never hits the 403 path).
  - serve.mjs serves with `Cache-Control: no-store`; when verifying UI stories in the browser a hard
    refresh is unnecessary — reloads always re-fetch fresh compiled modules.
---

## 2026-06-27 - US-002 (Point and Bounds value objects)
(Recovered: this run originally logged to the repo-root `progress.txt` by mistake; merged here.)
- What was implemented: Point and Bounds value objects in src/domain/
  - Point: immutable (x, y), equals(), translate() returning new Point
  - Bounds: immutable (minX, minY, maxX, maxY), width/height getters, contains(point, padding=0), grow(factor) expanding from centre
- Files changed:
  - src/domain/point.ts (new)
  - src/domain/bounds.ts (new)
  - tests/point.test.ts (new, 5 tests)
  - tests/bounds.test.ts (new, 9 tests)
- **Learnings for future iterations:**
  - The source tree was wiped (commit 29db285) so US-002..US-005 implementations were lost even though passes:true; each story may need re-implementation
  - Bounds.grow() expands from the bounding-box centre — keeps centroid stable during generation-space scaling
  - contains() padding inset: point at exactly padding distance from edge PASSES (>=, <=); one pixel inside padding strip FAILS
  - All domain imports between src/domain/ files must use relative paths with .js extension
---

## 2026-06-27 - US-002 (refinement)
- Refinement pass: added `Bounds.grow` shrink coverage (factor < 0.5)
- Files changed:
  - tests/bounds.test.ts (1 test added, now 10 tests; 16 total across suite)
- All 16 tests pass; typecheck clean
- No implementation changes needed — original code was correct
---

## 2026-06-27 - US-003 (Heading and Turn value objects)
- Implemented Heading and Turn value objects in src/domain/
  - Turn: TypeScript enum with Left="L" / Right="R"
  - Heading: class with static North/East/South/West instances (dx/dy unit step as readonly fields),
    apply(turn) method using a 4-element cycle array (Right=+1, Left=+3 mod 4)
- Files changed:
  - src/domain/turn.ts (new)
  - src/domain/heading.ts (new)
  - tests/heading-turn.test.ts (new, 14 tests: 4 unit vectors + 8 turn combinations + 2 enum values)
- All 30 tests pass; typecheck clean
- **Learnings for future iterations:**
  - Heading.apply() uses a static ALL array cycle; static fields initialise top-to-bottom so ALL must be
    declared after the four named statics (North, East, South, West) or it will contain undefined.
  - North points up in screen coordinates: step is (0, -1) i.e. decreasing y.
  - Left (CCW) = N->W->S->E->N cycle; Right (CW) = N->E->S->W->N cycle.
  - Use `assert.strictEqual` for reference equality on Heading singletons (apply returns the same static
    instance, not a new object) — deepEqual would also work but strictEqual is a tighter check.
---

## 2026-06-27 - US-003 (Heading and Turn value objects) — REFINEMENT
- US-003 already passed; all 7 acceptance criteria verified green.
- Refinement: added 2 cycle-invariant tests to tests/heading-turn.test.ts:
  - "4 Right turns from North returns to North" — verifies CW modular wrap
  - "4 Left turns from East returns to East" — verifies CCW modular wrap
  These test the `(idx + delta) % 4` arithmetic more thoroughly without changing implementation.
- Files changed: tests/heading-turn.test.ts (2 tests added; 16 Heading/Turn tests total, 32 total suite)
- All 32 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The 4-turn cycle test is a cheap invariant to add to any direction/turn system; catches off-by-one in modular arithmetic.
---

## 2026-06-27 - US-003 (Heading and Turn value objects) — REFINEMENT
- US-003 already passed; all 7 acceptance criteria remain green.
- Refinement: added 2 inverse-property invariant tests to tests/heading-turn.test.ts:
  - "Right then Left is identity for all headings" — loops all 4 headings, verifies R→L returns to origin
  - "Left then Right is identity for all headings" — loops all 4 headings, verifies L→R returns to origin
  These verify the mathematical property that Left and Right are mutual inverses — a regression guard the
  prior test suite lacked (individual per-heading turn tests verify specific directions; cycle tests verify
  modular wrap; this tests the inverse relationship directly).
- Files changed: tests/heading-turn.test.ts (2 tests added; 18 Heading/Turn tests total, 34 total suite)
- All 34 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The L+R=identity invariant is the strongest single test for a rotation system: it catches any asymmetry
    between Left and Right without requiring knowledge of specific turn directions.
---

## 2026-06-27 - US-004 (Segment value object)
- Implemented Segment value object in src/domain/segment.ts
  - Construction: accepts horizontal (same y) or vertical (same x); throws on diagonal and zero-length
  - `orientation`: "horizontal" | "vertical"
  - `length`: absolute value of the coordinate difference
  - `distanceFrom(point)`: minimum distance from point to segment — perpendicular distance when the foot is within range, endpoint distance when outside
  - `parallelOverlap(other)`: returns `{ overlapLength, separation }` for same-orientation segments; returns `null` for perpendicular pairs
- Files changed:
  - src/domain/segment.ts (new)
  - tests/segment.test.ts (new, 23 tests)
- All 57 tests pass (34 prior + 23 new); typecheck clean.
- **Learnings for future iterations:**
  - `parallelOverlap` returns `null` (not an error) for non-parallel pairs — callers check orientation before using the result; US-007 layout-rule predicates will filter on this.
  - `distanceFrom` uses `Math.hypot` for the off-corner case to avoid manual sqrt; precision is exact for integer inputs.
  - The `Orientation` type is exported alongside `Segment` so layout-rule code can reference it without re-declaring.
---

## 2026-06-27 - US-004 (Segment value object) — REFINEMENT
- US-004 already passed; all 6 acceptance criteria verified green.
- Refinement: added 6 tests to tests/segment.test.ts (23 → 29 Segment tests, 57 → 63 total):
  - Co-linear horizontal segments with overlap → separation=0, overlapLength=50
  - Co-linear vertical segments with no range overlap → separation=0, overlapLength=0
  - start/end points are accessible (layout-rules and renderer consumers use these)
  - distanceFrom returns 0 at start endpoint
  - distanceFrom returns 0 at end endpoint
  - distanceFrom is identical on both sides of the segment axis (symmetry invariant)
- Files changed: tests/segment.test.ts (6 tests added)
- All 63 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - Co-linear segments (same axis line, separation=0) must pass the layout-rule's "closer than 55px" check — separation=0 always violates it; ensure US-007 predicates treat co-linear segments as failing the spacing constraint.
  - `segment.start` and `segment.end` (readonly Point) are the canonical handles for US-007 and the renderer; the tests now confirm they are accessible.
---

## 2026-06-27 - US-004 (Segment value object) — REFINEMENT
- US-004 already passed; all 6 acceptance criteria verified green.
- Refinement: added 4 tests to tests/segment.test.ts (29 → 33 Segment tests, 63 → 67 total):
  - `distanceFrom` symmetry on vertical segments (mirrors existing horizontal symmetry test)
  - `parallelOverlap` with reversed-direction segments (start.x > end.x) — exercises `Math.min/max` path
  - Construction error for diagonal mentions 'diagonal' (asserts message text, not just that it throws)
  - Construction error for zero-length mentions 'non-zero' (same)
- Files changed: tests/segment.test.ts (4 tests added)
- All 67 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `parallelOverlap` is direction-independent because it uses `Math.min/Math.max` on coordinates internally;
    both forward and reversed segments produce the same result — confirmed by the new test.
  - Asserting error message text (e.g. `/diagonal/i`) is strictly tighter than bare `assert.throws(fn)`;
    prefer regex patterns on throw checks when the error message is part of the public contract.
---

## 2026-06-27 - US-005 (TurnSequence value object)
- Implemented TurnSequence value object and RandomSource port interface
  - `src/domain/random-source.ts`: minimal `RandomSource` interface (`nextFloat(): number`)
  - `src/domain/turn-sequence.ts`: immutable `TurnSequence` class; `turns: ReadonlyArray<Turn>`; factory `generate(waypointCount, random)` produces exactly `waypointCount - 2` turns; `get(index)` accessor; throws on `waypointCount < 2`
  - `tests/turn-sequence.test.ts`: 10 tests covering length math (N=10→8, N=90→88, N=2→0), Left/Right mapping (< 0.5 → L, >= 0.5 → R), determinism under a seeded stub, turn validity invariant, immutability, `get()` vs `turns[]` consistency, and error message text
- All 77 tests pass; typecheck clean
- **Learnings for future iterations:**
  - `RandomSource` interface (`nextFloat(): number`) lives in `src/domain/` so it can be referenced by both TurnSequence and the walk-generator without introducing a DOM/Node dependency; US-009 adds the seedable implementation in the adapters layer.
  - TurnSequence `turns` is `ReadonlyArray<Turn>` — TypeScript enforces immutability at compile time; no need for Object.freeze at runtime in a strict-typed codebase.
  - Stub a `RandomSource` in tests with a simple closure over a values array cycling via `i++ % values.length` — no external seeding library needed.
  - The `< 0.5` threshold is the canonical Left/Right split; exact boundary (0.5) maps to Right — document this if it matters for reproducibility.
---

## 2026-06-27 - US-005 (TurnSequence value object) — REFINEMENT
- US-005 already passed; all 5 acceptance criteria verified green.
- Refinement: added bounds checking to `get(index)` and 4 new tests (10 → 14 TurnSequence tests, 77 → 81 total):
  - `get()` now throws `RangeError` with "out of bounds" message for index < 0 or index >= length; previously silently returned `undefined` typed as `Turn` — a correctness hole the walk generator (US-010) would hit calling `seq.get(k-2)`.
  - New test: negative `waypointCount` throws (was untested; guard was correct but unexercised).
  - New test: exact `0.5` boundary maps to `Turn.Right` (documents the canonical L/R split precisely).
  - New tests: `get()` out-of-bounds throws `RangeError` for both `>= length` and negative index.
- Files changed:
  - src/domain/turn-sequence.ts (bounds check in `get()`)
  - tests/turn-sequence.test.ts (4 tests added)
- All 81 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `get(index)` now throws `RangeError` with "out of bounds" in the message — US-010 walk generator can rely on it rather than adding its own bounds check.
  - The exact 0.5 boundary mapping to `Right` is now a documented contract in a test; if US-010 ever changes the threshold it will be caught here first.
---

## 2026-06-27 - US-005 (TurnSequence value object) — REFINEMENT
- US-005 already passed; all 5 acceptance criteria remain green.
- Refinement: added 3 edge-case tests to tests/turn-sequence.test.ts (14 → 17 TurnSequence tests, 81 → 84 total):
  - `get(0)` on an empty sequence (N=2, length=0) throws RangeError — prior out-of-bounds tests only used non-empty sequences; this closes the N=2 boundary gap.
  - Exact sequence shape verification: generates with known stub values [0.1, 0.9, 0.3, 0.7] and asserts the output is [L, R, L, R] for N=6 — the determinism test only checked two calls match, not what the actual output is.
  - `waypointCount = 0` throws — prior throw tests covered 1 and -5; 0 was an untested case within the `< 2` guard.
- Files changed:
  - tests/turn-sequence.test.ts (3 tests added)
- All 84 tests pass; typecheck clean. No implementation changes needed.
- **Learnings for future iterations:**
  - The N=2 empty-sequence `get(0)` case is the sharpest test for the `index >= length` guard (0 >= 0 is not obvious at a glance); worth adding for any bounded-index accessor.
  - Exact sequence shape tests (specific input → specific output array) are stronger than determinism tests alone; add them when the mapping logic is non-trivial.
---

## 2026-06-27 - US-006 (Waypoint entity)
- Implemented the Waypoint entity in src/domain/waypoint.ts
  - `Waypoint.create(sequenceNumber, totalWaypoints, position, outboundTurn, wildcard)` — single validated factory
  - Terminal waypoints (seq 1 and N): enforced to have `outboundTurn === null` and `wildcard === false`
  - Interior waypoints (2..N-1): can have any `outboundTurn` (Turn | null) and `wildcard: boolean`
  - Wildcard semantics: `wildcard=true` with `outboundTurn=null` (the turn from the sequence is skipped, heading unchanged)
  - RangeError thrown for sequenceNumber out of [1..totalWaypoints] and for totalWaypoints < 2
  - All four fields exposed as readonly: sequenceNumber, position, outboundTurn, wildcard
- Files changed:
  - src/domain/waypoint.ts (new — Waypoint entity)
  - tests/waypoint.test.ts (new — 19 tests)
- All 103 tests pass (84 prior + 19 new); typecheck clean.
- **Learnings for future iterations:**
  - Wildcard waypoints store `outboundTurn: null` (the turn is skipped/not applied); the `wildcard` flag marks them. The turn sequence for the walk still has a value at that index but the walk generator discards it.
  - The single `create()` factory takes `totalWaypoints` so it can enforce the terminal/interior distinction. Two-factory alternatives (createTerminal/createInterior) are cleaner to call but make the "wildcards confined to 2..N-1" invariant harder to test in isolation.
  - Interior waypoints with `outboundTurn=null` and `wildcard=false` are not explicitly prevented — they are the last waypoint pattern. Only the combination of `wildcard=true` on a terminal is rejected.
---

## 2026-06-27 - US-006 (Waypoint entity) — REFINEMENT
- US-006 already passed; all 6 acceptance criteria remain green.
- Refinement: enforced the wildcard semantic invariant — `wildcard=true` with `outboundTurn !== null` now
  throws `"Wildcard waypoints must have no outbound turn (turn is skipped)"`. Previously the entity silently
  accepted an inconsistent combination (wildcard flag + non-null turn), which contradicts the "heading-unchanged/
  turn-skipped" semantics stated in the AC.
- Added 4 new tests (19 → 23 Waypoint tests, 103 → 107 total):
  - `wildcard=true` + `Turn.Left` throws (invariant test)
  - `wildcard=true` + `Turn.Right` throws (invariant test, Right variant)
  - Negative `sequenceNumber` (-1) throws with "out of range" (prior tests only checked 0 and > N)
  - `totalWaypoints = 0` throws (prior test only checked `totalWaypoints = 1`)
- Files changed:
  - src/domain/waypoint.ts (new guard: wildcard + non-null outboundTurn)
  - tests/waypoint.test.ts (4 tests added)
- All 107 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - The `wildcard=true` + non-null `outboundTurn` combination is now a hard construction error; US-010
    walk-generator must pass `null` as the outboundTurn when marking a waypoint as a wildcard.
  - When a wildcard skips a turn, the sequence still has a Turn value at that position — the generator
    simply passes `null` as the turn and `true` as the wildcard flag; it does NOT pass the sequence turn.
---

## 2026-06-27 - US-006 (Waypoint entity) — REFINEMENT
- US-006 already passed; all 6 acceptance criteria remain green.
- Refinement: added `totalWaypoints` as a stored readonly field and four computed getters:
  `isFirst`, `isLast`, `isTerminal`, and `isInterior`. These let any consumer (renderer, Walk
  aggregate, generator) query terminal/interior state from the entity itself rather than needing
  an external count in scope.
- Added 14 tests (23 → 37 Waypoint tests, 107 → 120 total):
  - `totalWaypoints` accessible as a field
  - `isFirst` true/false for first / interior
  - `isLast` true/false for last / interior
  - `isTerminal` true for first, true for last, false for interior
  - `isInterior` true for interior, false for first/last
  - Interior non-wildcard with `outboundTurn=null` is allowed (documents the spec boundary)
  - Minimum 2-waypoint walk: `isFirst`/`isLast` pair verified
- Files changed:
  - src/domain/waypoint.ts (totalWaypoints field, isFirst/isLast/isTerminal/isInterior getters)
  - tests/waypoint.test.ts (14 tests added)
- All 120 tests pass; typecheck clean.
- **Learnings for future iterations:**
  - `Waypoint` now carries `totalWaypoints` as a readonly field; US-008 (Walk aggregate) and US-013
    (renderer) can call `waypoint.isFirst` / `waypoint.isLast` / `waypoint.isTerminal` without any
    external count. Prefer these over comparing `sequenceNumber` directly.
  - `isInterior` is the logical inverse of `isTerminal`; use it to filter the waypoints that carry
    a meaningful `outboundTurn` or wildcard flag.
---

## 2026-06-27 - US-007 (layout-rules pure constraint predicates)
- Implemented `src/domain/layout-rules.ts`: six pure boolean predicate functions plus a composite
  `checkLayout` function returning a `LayoutViolation[]` list:
  - `noWaypointCirclesOverlap(waypoints)` — distance < 2*25px = fail
  - `noCloseParallelSegments(segments)` — parallel + overlapping range + separation < 55px = fail
  - `noSegmentCloseToNonAdjacentWaypoint(waypoints, segments)` — segment < 35px from non-adj centre = fail
  - `noSegmentThroughNonAdjacentWaypointCircle(waypoints, segments)` — segment < 25px from non-adj centre = fail
  - `turnLabelsClearOfNonAdjacentSegments(waypoints, segments)` — NE label (46px at 45°) < 8px from non-adj segment = fail
  - `allWaypointsWithinBounds(waypoints, bounds)` — uses `Bounds.contains(pos, 30)`
  - Exported constants: WAYPOINT_RADIUS=25, MIN_PARALLEL_SEPARATION=55, MIN_SEGMENT_WAYPOINT_CLEARANCE=35,
    TURN_LABEL_OFFSET=46, TURN_LABEL_CLEARANCE=8, BOUNDS_PADDING=30
  - `LayoutViolation { rule: string }` interface + `checkLayout(waypoints, segments, bounds)` composite
- Implemented `tests/layout-rules.test.ts`: 35 tests; positive/negative and boundary cases for all 6 constraints
- All 155 tests pass; typecheck clean
- Files changed:
  - src/domain/layout-rules.ts (new)
  - tests/layout-rules.test.ts (new)
- **Learnings for future iterations:**
  - Adjacency model: `segments[i]` (0-indexed) is adjacent to `waypoints[i]` and `waypoints[i+1]`.
    All other waypoint indices are "non-adjacent" for segment-proximity checks. US-008 (Walk aggregate)
    and US-010 (walk-generator) should use the same model when indexing.
  - NE label position: `(cx + 46*cos(45°), cy - 46*sin(45°))` in screen coords (y increases down).
    That is `dx = 46/√2 ≈ 32.527`, `dy = -32.527`. The minus sign on dy is critical.
  - `noCloseParallelSegments` only triggers when `overlapLength > 0`; parallel segments whose
    projected ranges do NOT overlap are exempt regardless of separation distance.
  - `noSegmentThroughNonAdjacentWaypointCircle` (25px threshold) is strictly subsumed by
    `noSegmentCloseToNonAdjacentWaypoint` (35px threshold), but both predicates exist as separate
    checks as specified; the composite `checkLayout` may report both.
  - Test fixtures: `Waypoint.create()` validates sequenceNumber/totalWaypoints/turn/wildcard but NOT
    position — tests can freely position waypoints and segments independently (the predicates use
    index-based adjacency, not geometric walk connectivity).
---
